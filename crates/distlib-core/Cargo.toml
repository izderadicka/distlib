[package]
name = "distlib-core"
description = "Domain types, identifiers, configuration and errors shared across distlib"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[features]
# Test-harness affordances that must not exist in a production build. Currently
# `RawMemberId: From<u64>`, which openraft's storage conformance suite requires.
testing = []

[dependencies]
iroh = { workspace = true }
getrandom = { workspace = true }
data-encoding = { workspace = true }
postcard = { workspace = true }
secrecy = { workspace = true }
blake3 = { workspace = true }
serde = { workspace = true }
figment = { workspace = true }
directories = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }

[dev-dependencies]
figment = { workspace = true, features = ["test"] }
# NodeAddr encoding canonically is a property the Raft log depends on, and the
# test that pins it belongs with the type.
postcard = { workspace = true }
proptest = { workspace = true }
tempfile = { workspace = true }
toml = { workspace = true }

[lints]
workspace = true
