# Deltas from the design plan

[`distlib-plan.md`](distlib-plan.md) is the original design and is **never edited**. This file is
the living record of every deviation from it, organised by the phase in which the deviation was
discovered.

**The rule:** any PR that changes a design assumption updates this file in the same PR. A delta
recorded weeks later is archaeology; a delta recorded alongside the code that depends on it is
documentation.

Each entry carries the section of `distlib-plan.md` it contradicts, what the doc says, what we
actually do, and why. "Carried forward" lists constraints discovered in one phase that a later
phase must design around.

---

## Cross-cutting

### C1 — iroh reached 1.0 (discovered in Phase 0, affects every phase)

The design doc was written against the iroh 0.10x series. Verified against crates.io and
docs.rs on 2026-08-23:

| §  | Doc says | Reality |
|---|---|---|
| §3 | "the iroh family moves fast (0.10x)" | **`iroh 1.0.3`** — stable and semver-committed. The compatible set is `iroh-blobs 0.103.0`, `iroh-gossip 0.101.0`, `iroh-docs 0.101.0`, `iroh-relay 1.0.3`, `irpc 0.17.0`; `iroh-docs 0.101` itself pins `iroh-blobs ^0.103` and `iroh-gossip ^0.101`, so the set is coherent. The exact-pin policy still stands, but the churn it guards against is much reduced. |
| §4.1, §8 | `NodeId`, `NodeAddr` | Renamed **`EndpointId`** (an alias of `PublicKey`) and **`EndpointAddr`**. |
| §3 | `discovery` | Renamed **`address_lookup()`** on the endpoint builder. |
| §5.1, §9 | "allowlist hook at connection accept" (mechanism unspecified) | iroh 1.0 provides **`EndpointHooks::after_handshake(&Connection) -> AfterHandshakeOutcome`**, which fires after the TLS handshake on both incoming *and* outgoing connections. `Reject { error_code: VarInt, reason: Vec<u8> }` closes the connection. This is the mechanism. |
| —  | MSRV unstated | The iroh family requires **Rust 1.91**. |

Three consequences that outlive Phase 0:

1. **The allowlist hook is endpoint-level, not per-protocol.** Phases 2–5 register
   `ProtocolHandler`s we do not author (iroh-blobs, iroh-docs, iroh-gossip). An allowlist check
   inside our own handler would protect `distlib/ping/0` and nothing else. `EndpointHooks` is the
   only place that covers all of them. **Do not move this check into a protocol handler.**
2. **`after_handshake` also gates our own outgoing connections.** This is correct for "do not talk
   to expelled members", but see the Phase 1 carry-forward below.
3. **`SecretKey::generate()` takes no RNG argument** in 1.0, so no `rand` dependency is needed.

---

## Phase 0 — Skeleton & transport

| # | §  | Doc says | We do | Why |
|---|---|---|---|---|
| P0-1 | §9 vs §8 | §9 names the binary crate `distlib-daemon`; §8 shows `crates/distlib/` | **`crates/distlib`** | §8's layout and the user-facing command name agree with each other; §9 was a slip. |
| P0-2 | §4.1 | keep `member_id` distinct from `node_id` so multi-device is possible later | **`MemberId` only — no `NodeId` newtype** | v1 defines them as equal. The name now shadows a type iroh renamed (`NodeId` → `EndpointId`), so introducing it buys a Phase 2 affordance at the price of permanent reader confusion. Phase 2 multi-device introduces `DeviceId` at the point it means something. |
| P0-3 | §9 | acceptance is "two containers on separate networks" | **in-process relay (`iroh::test_utils::run_relay_server`) plus `Builder::clear_ip_transports()`** | Makes the relay path structural instead of hoping loopback does not upgrade to a direct connection, and keeps the suite free of third-party infrastructure. `docker/` arrives in Phase 3 with the real binary. The in-process relay serves self-signed certificates, so the test endpoints need `CaTlsConfig::insecure_skip_verify()` and an `iroh-relay` dev-dependency — test-only, never on a production path. |
| P0-4 | §8 | integration tests live in `tests/` at the workspace root | `crates/distlib-net/tests/` | A single-crate test directory needs no extra scaffolding. Root `tests/` is reserved for the Phase 1+ multi-crate cluster harness that actually needs it. |
| P0-5 | §12 | openraft listed as a settled choice | **deliberately unpinned in Phase 0** | `0.9.25` (stable) vs `0.10.0-alpha.34` is a real decision with consequences, and Phase 0 does not depend on it. Decided at the start of Phase 1. |
| P0-6 | §2 | platforms are Linux, Windows and macOS | **CI tests Linux only** | Deliberate, for turnaround speed while the codebase is small. The matrix is kept as a one-element list so widening it is a one-line change. See the carry-forward below. |
| P0-7 | §5.2 | `item_id = BLAKE3(tag \|\| n \|\| sorted[ len(h_i) \|\| h_i ])` | **the per-element length prefix is dropped**: `BLAKE3(tag \|\| n \|\| sorted[ h_i ])` | Every `h_i` is a 32-byte BLAKE3 hash, so the concatenation is already unambiguous and the prefix distinguishes nothing. The forward-compatibility argument does not hold either: a later scheme admitting variable-length elements would carry its own domain tag, and the tag is what prevents a cross-version collision. The count is kept — one field, and it makes the pre-image self-describing. **This changes every item id**, which is free now and would not be once a catalogue exists. |
| P0-8 | §5.1, §9 | "allowlist hook at connection accept" | **two hooks, not one**: `before_connect` (refuse to dial a non-member) plus `after_handshake` (refuse a verified non-member) | `after_handshake` alone satisfies the acceptance criterion, but it fires only after packets have been exchanged, so an expelled member would learn the node is running. `before_connect` costs ~10 lines and closes that. `after_handshake` remains the authoritative check — it is the first point at which the peer's identity is proven rather than claimed — and is the only one that covers incoming connections. |

### Carried forward to Phase 1

- ~~**Outgoing connections are gated by the same allowlist** (consequence 2 of C1), so the join
  flow needs a bootstrap exemption scoped to the join ALPN.~~ **Discharged, and no exemption was
  needed.** The problem dissolved rather than being solved: `distlib join` writes the ticket's core
  nodes into `[consensus] core`, which *is* the joiner's bootstrap allowlist seed — so the joiner
  may dial them, and they admit the joiner because the group already committed a `MemberAdded` for
  it. Neither side is talking to a stranger. Worth recording because the anticipated fix would have
  punched a hole in the allowlist that turned out to be unnecessary.
- ~~Expulsion must close connections that are already open (§4.4).~~ **Discharged.**
  `Allowlist::changed()` is exposed and `AllowlistHooks::evict_expelled` consumes it, closing live
  connections to removed members with the same `NOT_A_MEMBER` code a refused handshake gets. The
  hooks keep a `WeakConnectionHandle` per peer, which is what the `EndpointHooks` docs prescribe
  for looking a connection up later without disabling close-on-drop.
- ~~**openraft version choice** (P0-5).~~ **Discharged** — `=0.9.25` with `storage-v2`, see P1-4.
- **Windows and macOS are unverified** (P0-6) — still open, and now carried into Phase 2. §2 claims all three platforms, but CI exercises
  only Linux. The first code that actually diverges is the `0600` key-file handling in
  `distlib-core::identity` — `cfg(unix)` permissions with a Windows fallback — so that fallback
  compiles and runs untested for now. Widen `.github/workflows/ci.yml`'s `test` matrix before
  claiming cross-platform support anywhere user-facing (README, release artefacts).

---

## Phase 1 — Membership log (Raft core)

**Complete.** The rows below are the record of where the implementation differs from the plan and
why — kept because §4 and §9 still describe the original, and a reader comparing the two needs the
answer. What is *not* finished is in [Carried forward to Phase 2](#carried-forward-to-phase-2) at
the end of this chapter.

| # | §  | Doc says | We do | Why |
|---|---|---|---|---|
| P1-1 | §4.2 | "Append-only log of **signed** events"; followers "verify the core's signatures" — the scheme is not specified | **Each event is signed by the member who proposed it**, in an envelope carrying `proposer` and `at`, verified when the event is applied | The transport already authenticates *who served us the log* (iroh connections are mutually authenticated), so what a signature adds is attribution of the entry itself: a compromised core node cannot invent a `MemberExpelled` and attribute it to somebody else. It gives §4.3's "proposer is recorded — auditability over ceremony" real weight for ~64 bytes and one verify per entry. It does **not** defend against a core node that refuses to serve entries or serves a stale prefix — that is Raft's problem, and outside §2's threat model either way. |
| P1-2 | §4.2 | `MemberAdded { invited_by }` and `MemberExpelled { proposed_by }` | **Both fields dropped** | The signing envelope already carries an authenticated `proposer`, which is the same member. Keeping both would let them disagree with no rule for which wins — and only the signed one means anything. |
| P1-3 | §4.2 | `MemberRecord` carries `joined_at` and `last_changed` | **Both fields dropped** | They are the proposer's clock, which nothing verifies and nothing keeps in step, so they can never be authoritative. Nothing derived from the log reads them, and they are not lost: each event's envelope carries `at`, so a UI wanting "joined on" reads it from the `MemberAdded` entry. Keeping them in the projection would invite exactly the mistake the `Timestamp` docs warn against — comparing timestamps to decide what happened first, when log order is the only truth. |
| P1-4 | §12, P0-5 | openraft listed as settled, version unstated | **`=0.9.25` with the `storage-v2` feature** | Latest stable. `0.10` has been `0.10.0-alpha.34` for months with no sign of landing, and the exact-pin policy exists precisely to avoid depending on something that can break without semver. `storage-v2` is not a preference: `impl<T> Sealed for T {}` is gated on it, so without that feature `RaftLogStorage` and `RaftStateMachine` cannot be implemented at all. Closes P0-5. |
| P1-5 | §4.1 | — | **`RawMemberId` in `distlib-core`; `MemberId` deliberately has no `Default`** | `openraft::NodeId` requires `Default`, which an ed25519 key cannot sensibly have — `PublicKey` validates that its bytes decompress to a curve point. Giving `MemberId` one would be a hazard, not a convenience: `#[serde(default)]` on any config or wire struct would then silently produce *a member* rather than an error, in a system where membership is the security boundary. openraft needs the bound only so its own `testing::Suite` can build placeholder log ids — a test-harness artifact, not a domain concept. `RawMemberId` is the unvalidated byte form, sited in core rather than consensus so the workspace has one such type and one validation point (`MemberId::try_from`) for ids arriving from any framework or wire. |
| P1-6 | §4.5 | "log + state machine persisted in redb" | **`redb = "=4.2.0"`** | Resolved against the tree rather than guessed: `iroh-blobs 0.103` and `iroh-docs 0.101` both use redb 4.2.0. (`iroh-docs` additionally pulls 3.1.3 behind its `redb-v2-migration` feature — not the live engine.) Matching it means Phase 2 adds no second storage engine. |
| P1-7 | §4.5 | "Snapshots: trivial (state = the full membership table; it's small)" | **a persistent state machine**: `apply` commits before returning, and snapshots exist to catch peers up rather than to recover from | The doc is right that the state is small, which is what makes this affordable — one commit per apply, and recovery is a read rather than a log replay from the last snapshot. openraft offers either arrangement; this is the one with less to reason about. |
| P1-8 | §4.2 | — | **a committed event the rules reject is skipped, not fatal** | By the time an event reaches the state machine Raft has committed it, so every node sees it. Returning a storage error would make one malformed proposal a simultaneous fatal failure on every node in the group. `MembershipState::apply` is deterministic and leaves state untouched on error, so skipping is equally consistent and merely loud: the event is logged at `warn` and the log id still advances. Proposals are validated before submission; this is the backstop. |
| P1-9 | — | — | **`distlib-core` gains a `testing` feature** enabling `RawMemberId: From<u64>` | openraft's conformance suite requires `NodeId: From<u64>` to mint ids in its fixtures. An integer is not a member, so the impl is gated out of production builds entirely. Note this is a *weaker* concession than `Default` would have been (P1-5): a feature flag could not have made `Default` safe, because the hazard there — `#[serde(default)]` inventing a member — lives in code paths a production build compiles. |
| P1-10 | §4.5 | "Snapshots: trivial" | **a stored snapshot is never allowed to move backwards** | openraft spawns `build_snapshot` onto its own task while the state machine worker keeps running, so a builder started at an older log id can still be in flight when a newer snapshot is installed from the leader. Overwriting blindly would regress `get_current_snapshot` — and because openraft purges the log up to an installed snapshot, the entries needed to bridge the gap are already gone, leaving the node unable to catch anyone up. `build_snapshot` therefore skips the store when a newer snapshot is present, while still returning what it built. |
| P1-11 | §5.1, P0-4 | `alpn::registered()` is "every ALPN this build accepts" | **it is the set [`crate::Node`] serves, and `configure` takes the ALPNs as a parameter** | An endpoint must advertise exactly what its router handles: negotiating a protocol and then having no handler for it means accepting the connection and refusing every stream. `distlib-net` cannot serve `distlib/raft/0` — the handler needs a `Raft`, which lives in `distlib-consensus`, and that crate depends on `distlib-net` rather than the reverse. So the caller that builds the router now says which ALPNs it serves. |
| P1-12 | §4.2, §4.5 | — | **the bootstrap seed is kept until a `GroupFounded` is applied, then the log wins outright** | The circularity the plan predicted, now in code: core nodes cannot replicate the founding entry without connecting, and cannot connect without an allowlist. So the follow task publishes the log-derived allowlist only once the group exists; before that it leaves the seed alone, and a node whose log has nothing to say would otherwise enforce an empty allowlist and never reach anyone. After founding the seed is never consulted again, so a member who was only ever in configuration stops being admitted. Both directions are pinned by tests, and each fails under the opposite mutation. |
| P1-13 | §4.3 | "That member submits `ProposeAdd` to any core node" | **a proposal made on a follower is forwarded to the leader**, over the existing `distlib/raft/0` ALPN | openraft's `client_write` does not forward: it returns `ForwardToLeader` and leaves it to the application. Without forwarding only the founder could ever grow the group, because every core node admitted afterwards is a follower, and so is a node that restarted while somebody else held the term. The forwarded reply carries a message rather than a typed openraft error — a follower has no use for the leader's own `ForwardToLeader` pointing back at itself. |
| P1-14 | §4.2 | — | **`R = Result<(), ConsensusError>`: a write returns the state machine's verdict** | Committing and applying are different things here. P1-8 makes a committed event whose rules do not hold a skip rather than a fatal error, so a successful `client_write` says only that the entry reached the log. Without the verdict, `propose` reported success for a change that never happened — and any member could fill the log with entries no node will ever apply. `ConsensusError` had to become serialisable to travel back, which also removed `postcard::Error` from a domain type. |
| P1-15 | §3, §5.1 | — | **connections are pooled per (peer, protocol) in `distlib-net`, not per peer** | ALPN is negotiated once in the TLS handshake: `Connection::alpn()` reads it from handshake data, and iroh's router resolves a handler once and gives it the whole connection. Streams multiplex within a protocol, never across, so raft and the phase 1b log protocol each hold their own connection to a peer. That is iroh's model rather than a choice — avoiding it would mean multiplexing our protocols under one ALPN with our own dispatch. The cost is bounded: iroh keeps address lookup, hole punching and path selection per *remote*, so a second connection to a known peer is a handshake over an established path. `ping` deliberately does not pool — a liveness probe over a cached connection answers the wrong question. |
| P1-16 | §3, §4.3 | `net.allowlist` configures who a node talks to | **`net.allowlist` is gone; `[consensus] core` replaces it** | The allowlist is now derived from the log, so configuring it would be configuring a cache. What configuration still has to supply is the *founding* core group, and only until a group exists: founders cannot replicate the first entry without reaching each other, and cannot reach each other without an allowlist. So `[consensus] core` is read once, before `GroupFounded` applies, and never again. Unlike anything else that names a member it carries addresses, because the log that would otherwise supply them is exactly what those addresses are needed to fetch. No migration handling for the old key: nothing was ever released with it, so `deny_unknown_fields` refusing it is enough. |
| P1-17 | §9.1 | `distlib init-group` founds a group | **`distlib run --found-group` founds it instead** | Two constraints rule out a standalone command. redb takes an exclusive file lock, so a second process cannot open the node's database while it runs — `init-group` would have to be run with the node stopped. And the founder must stay up afterwards to replicate what it wrote, which a command that founds and exits cannot do. As a flag on `run` both fall away: the founder starts, founds, and serves. The other founders just `run`. |
| P1-19 | §9.1 | — | **`distlib whoami` added** | Founding needs every founder's id and address before any group exists to ask, so the exchange happens out of band — and nothing printed them in a form that could be pasted. `whoami` creates the identity if there is none and prints the `[consensus] core` line, rendered by the same code that writes the starter config so the two cannot drift. It prints no address when `bind_addr_v4` has no fixed port: an OS-chosen port is gone after the next restart, and a line the founder pastes has to keep working. It never prints the secret key. |
| P1-20 | §4.2, §4.3, §4.4 | membership events are committed by the core group; the only stated rule is that admission is invitation-only | **Per-event proposer rules, enforced in `MembershipState::apply`**: a `PledgeChanged` may only be proposed by the member whose pledge it is, and a `CoreGroupChanged` only by a current core member. `MemberAdded` and `MemberExpelled` stay open to any member, as §4.3 and §4.4 say. | Being a member was the only check, which left two holes. A pledge is a promise about the proposer's *own* storage and §5.5 makes custodian assignment depend on it, so anyone rewriting anyone else's could move everybody's data. The core group is the set of Raft voters, so a non-voter rewriting it could remove every voter but themselves. The rules live in `apply` because that is the one place every node runs identically — a check at the API or in a protocol handler would be a second opinion only that node holds, and two nodes disagreeing about whether an entry applied is a split membership. |
| P1-21 | §4.2 | — | **Every proposal carries the membership it was made against**: `MembershipState` tracks `changed_at`, the log index of the last entry that changed it; the signing envelope carries the value its proposer saw, and `apply` refuses a mismatch. | Raft's guarantee stops at the voters. §4.2's non-core followers *pull* the log, so a follower is eventually consistent and can act on a group that has moved on. This makes that self-announcing: a stale node finds out when it tries to propose, rather than silently proposing against a membership that no longer exists. The log's own index rather than a counter of our own, so there is one monotonic number and an error can name an index that `members` also reports. The last *membership change* rather than the log head, because Raft commits a blank entry at every leader election and comparing heads would invalidate every proposal in flight during one. |
| P1-22 | §4.2, §4.5 | one dedicated ALPN for the Raft RPC; `distlib/memberlog/0` for log distribution to followers | **`distlib/raft/0` carries openraft's RPCs and nothing else, and is refused to any peer that is not a current voter. Proposals moved to `distlib/memberlog/0`**, which every member may speak. | The two have different audiences, and one ALPN serving both meant a node that accepted proposals from any member was also serving `Vote` and `AppendEntries` to them — so any member in the allowlist could disrupt a term. Splitting them makes the audience the protocol boundary. A node whose voter set is empty accepts from the members it was *configured to found with*, and only until the log says a group exists. "Voter set empty" alone was the first answer and was wrong: a node that is never initialised has an empty voter set for its whole life, so it would have served consensus to every member in its allowlist forever — enough for one member to send it a self-signed `GroupFounded` and make it evict the real group. The gate is re-checked per RPC rather than once per connection, because the voter set moves under a long-lived connection and §4.4 makes the same argument for the allowlist: refusing the *next* connection is not enough while the current one is open. |
| P1-23 | §4.5 | "Core group membership changes use openraft's joint-consensus mechanism, recorded as `CoreGroupChanged`" | **Deferred: `CoreGroupChanged` changes the projection only.** No `raft.change_membership` call exists, so committing one does not promote or demote a Raft voter — **and it cannot change a core node's address either, which is the sharper half.** | The event, its authorisation (P1-20) and its projection are all in place; what is missing is the consensus half. Worth stating rather than leaving implied, because P1-20's rationale — "a non-voter rewriting it could remove every voter but themselves" — reads as though the committed core set governs Raft, and today it does not: `RaftProtocol` asks openraft's own membership, which after `initialize` never changes. Joint consensus has failure modes of its own and deserves its own change rather than being tacked onto an authorisation fix. Until then a group's voters are its founders. **And their addresses are frozen at founding.** `raft.initialize` is the only call that ever writes the voter map, addresses included; nothing rewrites them, and they are what every core node dials (`RaftClient::endpoint_addr`) and what `core_addresses` hands to every join ticket. With `relay_mode = "default"` this is harmless — `presets::N0` configures DNS and pkarr lookup, an empty `NodeAddr` means "find them by member id", and a relay routes by node id rather than IP. With `relay_mode = "disabled"` and explicit `addrs` — a LAN or offline group — the recorded socket is the *only* way to reach that node, so **a core node that changes IP or port is out of its own group permanently**, with refounding the only way back. Whatever implements `change_membership` must therefore be able to submit a changed address for an existing voter, not only add and remove ids: it is the same openraft call, taking the same node map, and treating it as a voter-set feature alone would leave the more likely failure unfixed. |
| P1-24 | §9, §7.1 | `distlib-api` (axum: JSON-RPC + SSE + embedded UI) is phase 3 | **The JSON-RPC half arrives in phase 1b**, with the `group.*` and `node.status` methods from §7.1. SSE, `library.*` and the embedded UI stay in phase 3. | Phase 1 needs a way to commit a `MemberAdded` from outside a test: the running node holds the redb lock and the Raft, so no second process can reach them, and "stop the node to add a member" is not a group anyone can run. The alternative was a throwaway control channel — a Unix socket was considered and rejected — which would have been a second thing to discard the moment §7.1 arrived. Method names are §7.1's verbatim, so phase 3 extends this rather than renaming it. |
| P1-25 | §7.1 | "localhost by default" | **A bearer token always; loopback by default.** The token lives in `<data-dir>/api.token` at mode 0600, held in `secrecy::SecretString` and never logged. `[api] enabled` switches the listener off, and `[api] bind_addr` may be set to anything — binding off loopback warns rather than refuses. | Whoever can call this makes the node propose as itself. Narrower than holding the node's key — nothing proposed escapes the group's rules, and every proposal is signed and attributed — but not nothing. Loopback alone would trust every process on the machine, so the token is unconditional. The reverse does not hold: a node on a server or in a container has to be reachable from elsewhere, so loopback is the default and not a constraint. **There is no TLS**, so a remote listener wants a reverse proxy until there is; TLS and whatever authentication belongs beside it come with phase 3's UI. Also: a second node on one host needs its own `[api] bind_addr`, exactly as it needs its own `[net] bind_addr_v4`. |
| P1-26 | §3 | — | **hyper, not reqwest, wherever this API is called** | reqwest's `rustls` feature pulls `aws-lc-rs` — a C toolchain — to satisfy a TLS stack that a loopback HTTP call never uses, and feature unification with iroh's copy makes the no-provider build panic on the first request. hyper and hyper-util are already in the tree via axum, so this costs zero new packages. The caller lives in the tests until the CLI needs one: a client in the crate with no consumer is public surface committed to before anything asks for it. |
| P1-27 | §9.1, §7.1 | `group.members` / `group.propose_add` / `group.propose_expel` / `group.pledge_set` are API methods | **`distlib admit`, `distlib expel` and `distlib pledge` on the command line**, and `members` and `status` ask the running node before reading its database. | A group that could be founded and never changed was not usable by hand. Verbs rather than `member add` and `member expel`, so there is no `member`/`members` pair a tired typist can confuse. Asking twice is what retires the old "stop the node first" rule: only one of the two sources can answer at a time — the node holds the database exclusively while it runs — so trying the API first and the files second works whether or not it is up, and `status` gains the Raft state and current leader, which only a running node knows. |
| P1-28 | §4.2, §4.5 | "peers fetch missing suffix from any core node over a dedicated ALPN" | **`distlib/memberlog/0` gains `From { cursor }`**, answering with the events in `(cursor, up_to]` *and their log indices*, where `up_to` is the serving node's **applied** index rather than the last event's. Every answer also carries the current core group **with addresses**, and the leader if known. | Three things the sketch leaves open. Indices travel because `MembershipState::apply` needs one — the freshness check (P1-21) is against a log index, so a follower folding without them could not compute it. `up_to` is the applied index because most entries carry no membership event: a cursor taken from the last event would re-request the gap between them forever, and only entries the state machine has applied may be served at all, since anything beyond is committed but not yet part of anybody's membership. Addresses travel because §4.5 says `CoreGroupChanged` tells followers where to fetch from, and our variant carries member ids and none — so a follower could learn *who* to ask and still have no way to reach them. |
| P1-29 | §4.5 | "Snapshots: trivial (state = the full membership table; it's small)" | **A follower whose cursor predates the purge watermark is told `TooFarBehind`, not sent a snapshot.** | Serving the state itself is a second transfer path, a second thing for a follower to verify, and a second way for the two to disagree — for a case that needs 5,000 membership events first, since that is openraft's snapshot threshold and nothing purges before one. Told plainly rather than left to fail obscurely, so when a group does get there the error names the problem. **Known gap, not a decision to leave it there**: a follower in this state stays stuck, and the only way out today is to delete its data directory and rejoin. Serving the membership state — §4.5 says it is small — is the fix, and it belongs with whatever else needs snapshot transfer rather than being bolted onto the entry path. |
| P1-30 | §4.2, §9.1 | "All other members are *followers of the log*" — how a node knows which it is, is not stated | **The role is inferred, never configured**: a node listed in `[consensus] core` votes, and every other member follows. Before a group exists that reading comes from config; once `GroupFounded` is applied it comes from the log, so a node the group was founded without is a follower whatever its config claims. | The same rule the allowlist already follows, and for the same reason — a node cannot read a log it has no way to reach, so configuration bootstraps and the log then wins. A `role = ` key would be a third thing to keep in step with the other two, and the first thing to go stale. The role is fixed at startup: promotion by `CoreGroupChanged` needs a restart, which costs nothing today because that event does not move Raft's voters anyway (P1-23). |
| P1-31 | §4.2 | — | **A fetch is bounded at 5s where a proposal is bounded at 45s** | Different questions. A proposal has one place to go and waiting beats failing; a fetch has as many places to go as there are core nodes, and the log is small. Sharing the propose timeout left a follower stuck on a dead source for most of a minute — still enforcing a membership it knew might have moved — which is how this was found: the rotation test timed out. |
| P1-32 | §4.2, §9 | "committed entries announced via gossip"; §9 puts iroh-gossip in phase 4 | **iroh-gossip arrives now, for log announcements only.** Core nodes broadcast their applied index on the group's topic when the membership changes; followers listen and fetch. Availability heartbeats and challenge results stay in phase 4. | §4.2 puts this in phase 1 and the alternative does not scale: §2 allows thousands of members against three to seven core nodes, and having each of them poll briskly or hold a subscription open does not survive that. The topic is the group id — it is already the group's name, and deriving a second one would be another thing to agree on. iroh-gossip's version number trails iroh's, but 0.101 is built against iroh 1.0.3, so this adds no second endpoint stack. |
| P1-33 | §4.2 | — | **The announcement is a hint and carries no authority.** It says only "the log reaches index N", is not signed, and a follower acts on its own cursor rather than on the number. | Lying high makes followers ask a core node and find nothing; lying low is ignored. Everything that matters is verified when the entries arrive — signed events, folded by the rules every node applies — so there is nothing here worth protecting. Gossip is also best-effort: an announcement made before a follower's subscription is live is simply lost, which is why the follow loop keeps its own timer. That timer went from 5s to 30s now that it is a backstop rather than the mechanism. |
| P1-34 | §4.2 | — | **An announcement carries the index it reaches, and a follower ignores one it has already passed.** Waking without it meant a round trip per announcement per follower. | The load gossip was brought in to remove would otherwise come straight back through the other door: every core node's announcement would have every follower ask it a question whose answer it already had. The index is still only a claim — it is compared against this node's own cursor and used for nothing else — and a hint that carries no index (`Lagged`, a new neighbour) means "look", since there is no way to tell what was missed while unable to hear. |
| P1-35 | §4.2 | — | **Known: two mechanisms follow the log where one should.** Gossip announces, and a timer polls every 30s regardless. The polling is the guarantee behind a best-effort broadcast, but it keeps a standing load on core nodes that gossip was meant to remove. | The direction, not built here: catch up once at startup, then rely on announcements; treat a failed operation — a proposal refused as stale — as its own signal to refresh and retry, which `propose` already does; and make the safety poll optional with a period carried in the log itself, so every member agrees on it and it can be hours or days rather than seconds. Recorded now because the 30s timer looks deliberate and is really a placeholder for that. |
| P1-36 | §4.3 | "New member receives a **join ticket** (group ID + core node addresses + relay config), connects, fetches the log, starts syncing" | **`distlib ticket` and `distlib join <ticket>`**, one pasteable base32 line. `join` writes the directions into the config and stops there; `distlib run` is what starts following. | Two commands rather than one because they are two different acts: `join` changes configuration, and a command that silently began serving a group would be a surprising thing to have run. The ticket is **not a credential** — anyone who can reach a node's API can ask for one, and holding it grants nothing, since admission is a committed `MemberAdded` and until then the allowlist refuses the holder like anybody else. It is directions, which is why it can be pasted into a chat window. A prefix marks it so a later format fails as the wrong version rather than as corrupt data. |
| P1-37 | §4.3 | — | **`join` refuses a ticket for a different group than the one this node already follows.** | Replacing the bootstrap set would leave the node enforcing one membership while fetching another. Starting over is a legitimate thing to want, so the error names the data directory to delete rather than silently doing it. |
| P1-38 | §4.5 | — | **Reaching a peer is bounded separately from waiting for its answer**: three seconds to connect, against forty-five for the whole exchange. | Found by the Phase 1 acceptance test, not by reasoning. Killing the leader and immediately proposing hung for the full exchange timeout: the proposer's Raft still named the dead node, the forward tried to connect to it, and each of the three attempts spent forty-five seconds discovering it was gone. The retry loop exists precisely to re-ask who the leader is after a stale hint, and a connect timeout sized for a commit defeats it. The two waits answer different questions — a live leader may legitimately take most of `COMMIT_TIMEOUT` to reply, but a peer that cannot be *reached* in a few seconds has gone. The same distinction P1-31 drew for fetching, which should have been drawn here at the same time. |
| P1-39 | §4.2 | "committed entries announced via gossip" | **Every address this node learns is written to an address book on the endpoint**, so a member is dialable by id alone. Fed from three places: the configured core group at startup — which is also a ticket's core group — Raft's `StoredMembership` whenever the membership changes, and the core group carried in every memberlog answer, which is the only one a follower has. | iroh resolves an endpoint id only if the caller supplies an address or a lookup service can find one. Our own protocols always supply one, so this never came up; iroh-gossip subscribes with bare ids and cannot, and `relay_mode = "disabled"` builds on `presets::Minimal`, which configures no lookup at all. So the only peers gossip could reach were those the endpoint had already spoken to for some other reason — and Raft connects the leader to each voter and never one voter to another, so a three-node group's mesh collapsed to a star centred on the leader, with a `dial failed: No addressing information available` for every other edge. Found by hand, not by a test: the suite runs `Minimal` with relays disabled and asserts nothing about logs. A follower showed the shape of it most clearly — it holds every core node's address, from its ticket and from every memberlog response, and dials them happily when it rotates, but had no way to hand any of them to gossip. The addresses were in the process; they just could not reach the component that needed them. Its role is bootstrap and promptness rather than possibility, which a later test made precise: gossip distributes peer addresses in-band, so voters that reach the swarm through the leader do eventually resolve each other without the book — a retrying test kept passing with the book's writer deleted, and had to be rewritten to wait for the book and then dial once. What has no other route is the first contact, and a follower, which nothing ever dials. The book is `iroh::address_lookup::memory::MemoryLookup`, added to the endpoint *after* it is bound — the lookup services take `&self` — so nothing that builds an endpoint has to thread one through, and whoever learns addresses owns it. Not a security boundary and it does not need to be: being resolvable is not being admitted, the allowlist still decides that from the log, and the worst a wrong entry costs is a wasted dial. |
| P1-40 | §4.4 | "expelled member keeps what they already downloaded; this is by design" | **An expelled follower works out that it has been expelled, stops following, and the process exits non-zero.** It learns it the only way it can — every core node that answers refuses it — and a refusal is told apart from a peer being unreachable, because they mean opposite things: try somebody else, against you are not in this group. | Found by running the acceptance criteria by hand. An expelled member never receives the entry expelling it — the group stops talking to it the moment that entry commits — so it kept asking its sources about once a second for the life of the process. That is a node doing nothing useful, and it puts a refused connection into every core node's log every second, which is the group being denied service by somebody who is no longer in it. Distinguishing refusal from silence is what makes the answer safe: unreachable nodes get no vote, so one core node being down while the others refuse still ends the loop, and a node that has *never* held the log is not expelled but unadmitted — it waits, quietly, at a minute rather than a second. Usually prompt, because gossip reports a neighbour going away and that is exactly what the group closing its connections looks like — milliseconds when the mesh is up, against the thirty seconds the loop's own timer would take, which is the fallback when it is not. Exiting rather than idling is the node saying what happened: `distlib run` fails with a message naming the situation, and the operator decides whether to delete the data directory or keep it and rejoin. |
| P1-41 | §3, §4.4 | — | **Refusals are summarised in the log: one line per peer per minute, carrying how many attempts went unwritten**, with the table of refused peers bounded. iroh's own per-connection accept warning is turned down in the default filter. | The audit trail of who tried to reach a closed group is worth keeping, but at one line per attempt *the peer decides how much this node writes* — and the peer may be hostile, or merely an expelled node retrying. Two lines per second per attacker fills a disk without any traffic worth the name. Summarising keeps every peer that tried and how hard, at a rate this node chooses. The bound on the table is the same argument applied to memory, since the keys come from strangers: past the cap the peer quiet longest is dropped, which costs one extra line if it comes back. iroh logs its own warning for every connection its router fails to accept, and a refused non-member is exactly that — it says nothing our line does not, so `iroh::protocol=error` is in the default filter, and `-vv` puts it back. |
### Carried forward to Phase 2

Phase 1 is complete against §9's acceptance criteria, which run as a test on every commit
(`crates/distlib-consensus/tests/acceptance.rs`). Four things it deliberately did not finish:

- **`CoreGroupChanged` moves nobody, and nothing can move an address** (P1-23). The event commits,
  the projection updates, and Raft's voter set does not change — no `raft.change_membership` call
  exists. **A group's voters are its founders, at the addresses they were founded with.**
  `raft.initialize` writes that map once and nothing rewrites it, so in a group without address
  lookup (`relay_mode = "disabled"`, explicit `addrs`) a core node that changes IP or port is out
  of its own group for good. **The wiring must take a changed address for an existing voter, not
  just add and remove ids** — same call, same node map, and the address case is the one a group
  will hit first: machines get renumbered far more often than founders get replaced.
- **A follower too far behind cannot recover** (P1-29). Purging past its cursor leaves it
  `TooFarBehind` with no way back but deleting its data directory and rejoining. It takes 5,000
  membership events to reach, since nothing purges before openraft snapshots — a real gap with a
  long fuse. The fix is serving the membership state itself, which belongs with whatever else
  needs snapshot transfer rather than bolted onto the entry path.
- **Two mechanisms follow the log where one should** (P1-35). Gossip announces and a 30-second
  timer polls regardless. The timer is the guarantee behind a best-effort broadcast, but it keeps
  a standing load on core nodes that gossip was meant to remove. The direction: catch up once at
  startup, rely on announcements, treat a failed operation as its own signal to refresh, and make
  the safety poll optional with a period carried in the log so every member agrees on it.
- ~~**Windows and macOS are still unverified** (P0-6), unchanged since Phase 0.~~ **Discharged** in phase 3 — see P3-1.

## Phase 2 — Catalogue & library basics

*In progress.* The sequencing plan — sub-phases, PR boundaries, per-PR acceptance, and
which carried-forward items it takes — is [`plan-phases/phase-2-catalogue.md`](plan-phases/phase-2-catalogue.md).
That document is sequencing only: **deviations from the design still land here**, in the PR that
causes them.

Four items carry in from Phase 1 — see
[Carried forward to Phase 2](#carried-forward-to-phase-2) above. The phase-2 plan says explicitly
which are taken (P1-23) and which are deferred, and why.

| # | §  | Doc says | We do | Why |
|---|---|---|---|---|
| P2-1 | §9 | Phase 0 gives the binary `init` (keygen), `run` and `status`; nothing says which of them may create an identity | **`run` refuses a data directory that holds no identity**, and its first log line names the data directory and the config file it resolved — `(absent, using defaults)` when there is none. `init` and `whoami` still create one; `distlib-core` gains `identity::load_secret_key` beside `load_or_create_secret_key`, and the split is the whole fix. | Found by running the acceptance criteria by hand. A mistyped `--data-dir` produced a node that started perfectly: `run` minted a key in the empty directory, read defaults because the config file was not there either, and came up listening on an OS-chosen port with relays enabled under a member id nobody recognised. Nothing in the log said which directory or config file it had used, so the only way to notice was to recognise the *shape* of the symptom. Both halves are needed and neither is sufficient: refusing without the log line still leaves a healthy-looking node when the directory exists but the config file does not, and logging without refusing leaves the operator to spot one line among the startup noise. **Consequence for phase 3's container work:** an image starting from an empty volume must run `distlib init` (or `join`) before `run` — a fresh volume is no longer enough, deliberately, because a container that silently regenerates its identity on an empty mount has silently left the group. |
| P2-2 | §4.2, §4.5 | `GroupFounded { founders }` and `CoreGroupChanged { core }` carry member ids; addressing is not mentioned, and §4.5 leaves core-group changes to openraft's joint consensus | **Both events now carry an address per core node**, and `MembershipState` keeps the core group as a `BTreeMap<MemberId, NodeAddr>` rather than a set. `core_addresses()` reads that projection instead of openraft's `StoredMembership`. **No `change_membership` call yet** — P1-23 stays open, and this is the half it needs first. | P1-23's sharper half is that `raft.initialize` is the only call that ever writes the voter map, so the addresses in it can never change. Enacting a change means handing openraft a node map, and the log had nowhere to put one: `CoreGroupChanged` named ids only. Doing it as one wire change rather than two was deliberate — the alternative ships an intermediate state where a voter can be added with no address, which under `relay_mode = "disabled"` produces a voter nothing can reach. That is worse than today's "cannot change at all". Two consequences beyond the wiring. **A follower now knows where the core group is from the log itself**: `core_addresses()` used to read `StoredMembership`, which a follower never has — it runs no Raft — so a follower answered "no core nodes" about the group it was following, and would hand out a join ticket naming nobody. **And a core group named twice is refused rather than folded**: two addresses for one member has no single answer, and collecting into a map would silently keep whichever came last. Founding hashes ids only, unchanged: an address is not identity, and a group re-founded on a different port is the same group. |

| P2-3 | §4.5 | "Core group membership changes use openraft's joint-consensus mechanism, recorded as `CoreGroupChanged`" | **A `CoreGroupChanged` is now enacted in Raft — for a changed address and for a removal. Promotion is refused at apply time**, with `ConsensusError::PromotionUnsupported`, until a node can begin voting without restarting. Enactment is a reconciliation loop on the leader (`raft::core_group`) that diffs openraft's voter set against the projection rather than reacting to the event. | Closes the half of P1-23 that had a live failure mode: with `relay_mode = "disabled"` the recorded socket is the only way to reach a core node, and `raft.initialize` was the only call that ever wrote it, so a renumbered machine was out of its own group for good. **A diff rather than a command** is what makes it recover: a change interrupted by a lost election, a crash or a restart is a gap the next pass finds, with nothing extra persisted and no half-finished state to resume. It also fixes a bug that was there all along — `MemberExpelled` already dropped a member from the projection's core while openraft kept counting them as a voter, so expelling core members silently ate the fault tolerance they provided. **Promotion is refused rather than enacted** because a node serves `distlib/raft/0` only if it *started* as a voter (P1-30): promoting one now would add a voter that counts toward quorum and can never answer. Refusing the event is what keeps the projection and Raft from disagreeing — an addition never commits, so there is nothing to disagree about. This retires P1-30's justification that promotion-needs-a-restart "costs nothing today", and the rule goes when the runtime role change lands. **On `SetNodes`:** openraft's own documentation says "do not use unless you know what you are doing", because pointing a voter's address at a different node lets two quorums form and elect two leaders. That hazard cannot arise here. `RaftClient::endpoint_addr` builds `EndpointAddr::new(member.endpoint_id())`, so the socket is a hint the QUIC handshake overrides: a wrong address reaches *nobody*, never the wrong somebody. openraft names "ensure connection to the correct node" as the `RaftNetwork` implementation's responsibility in the same document, and iroh's mutually-authenticated handshake discharges it structurally. **Known gap:** the loop also wakes on leadership changing, so a node that becomes leader finishes what the last one did not. That arm is argued rather than tested — removing it fails nothing — because creating a gap needs a committed change, committing one needs a leader, and that leader's own loop closes the gap before another node could be asked to. The case that deserves a test is a leader removing itself, where openraft leaves a joint config behind if leadership is lost before the uniform config commits. |

| P2-4 | §4.3, §4.4 | §4.3 has "any member may propose adding a new member"; §4.4 step 1 has any member submit a removal, and step 2 has it "require acknowledgment by a configurable quorum of core nodes (default: majority) before commit" | **Step 2 exists, and covers admission too. One rule, applied to every membership event: changing who votes takes a majority of the core group, and everything else takes one core member.** `MemberAdded`, `MemberExpelled` and `CoreGroupChanged` become proposals that sit in `MembershipState::pending` until enough core members have applied an `Approved { proposal }` naming the log index they were made at. A core proposer's own proposal counts as their approval, so `distlib admit` and `distlib expel` run against a core node stay one step. **The quorum is computed, not configured.** **This supersedes P1-20's "`MemberExpelled` — open to any member, as §4.3 and §4.4 say"**, which was true of step 1 and silent about step 2. | Two things gave it teeth. **P2-3 made expulsion real**: removing a core member now genuinely removes them from openraft's voter set, so a run of single signed proposals shrank the group's ability to commit anything. **And the empty-core guard was bypassable**: `CoreGroupChanged` refuses to empty the core group and has a test saying so, while `MemberExpelled` walked straight past it — verified by probe, `Ok(())` and zero voters. A group with no voters can never commit anything again, including the re-admission that would fix it. Note what the case really is against §2's threat model, which assumes members do not attack the protocol: not an attacker but a **foot-gun**, since a mistyped id in `distlib expel` removed a voter and nothing asked. Four calls worth recording. **The floor that was planned is not there, because the thresholds subsume it**: expelling the last voter needs a majority of a core of one, the only core member is the one being expelled, and the target cannot approve their own expulsion — so it can be proposed and never decided. Verified by mutation: deleting any one of those three rules re-opens the hole and `the_last_voter_cannot_be_expelled` fails, while a separate floor would have been a line no test could reach. **Only approvals from current voters count**, on the same principle that puts the threshold against the current core: an approval from somebody since demoted is not a voter's agreement. **Repeat approvals are idempotent rather than refused**, because a proposal can become sufficient without anyone approving it — a shrinking core lowers the threshold — and nothing re-examines a pending proposal on its own; letting an existing approver say so again is what makes that reachable. **Duplicate proposals are accepted, not refused**: two members expelling the same peer split their approvals and neither reaches its threshold, which is a self-healing wart, where refusing would leave a stuck slot that only a withdrawal event could clear. **Superseded by 2.2-3** — that reasoning holds for two people proposing the same thing once, and understates one person proposing it repeatedly; and the claim made alongside it, that what stays pending is "bounded by how many people are genuinely under discussion", is false for `CoreGroupChanged`, whose pending proposals nothing prunes. 2.2-3 makes it one proposal per subject and expires the rest by log-index distance. **Consequences.** *Configurable* cannot mean a config-file key — the fold has to reach the same verdict on every node, so a per-node setting would split the membership; it means a policy event in the log, which §5.5's weight cap needs identically, so it gets built once, there. *A two-voter core group can no longer remove one of its voters*: a majority is two and the one being removed does not get a say. That is a genuine behaviour change and not a pre-existing condition — two healthy voters commit normally and simply cannot shrink — and it is the price of "one member cannot seize a group"; the way out is to grow to three, or for the departing voter to agree to a demotion, which they may, since standing down is resigning rather than being removed. *A core member cannot propose their own expulsion either*, so a graceful exit is demote-then-expel. *`MembershipState`'s persisted encoding changed* — a data directory written before this will not load, and a dev cluster has to be re-founded. |

| P2-5 | §7.1, §4.4 | §7.1 lists `group.propose_add` / `group.propose_expel` / `group.members` / `node.status`; nothing names a way to see or decide a proposal, because §4.4's step 2 was never built | **Three methods added — `group.pending`, `group.approve`, `group.withdraw` — and `node.status` gains a pending count.** `MembershipEvent::Withdrawn { proposal }` joins the log, **withdrawable by its own proposer alone**. `MembershipNode::propose` now answers with the log index its entry was applied at, carried across the wire by `ProposeOutcome::Applied { index }`, and `group.*` proposals report `applied` and what a waiting one is waiting for. | The surface half of P2-4: 2.2-1 made a proposal something that can wait, and left every command reporting as though nothing ever did. `distlib admit` run by a follower printed `admitted`, the admission then waited for a core member with nothing saying so, and the joiner holding the ticket could not connect — a silent failure invented by the previous PR and closed by this one. **The index is the mechanism**, and it is why `propose` changed shape: whether a committed proposal took effect is a question about `MembershipState::pending`, keyed by the entry's own log index, and matching on the event's content instead would be ambiguous exactly when it matters, since two proposals can say the same thing. Deliberately **not** a third `ProposeOutcome` variant saying "pending" — that is a fact about the state rather than about what became of the entry, and putting it on the wire would be the same fact in two places, free to disagree. **Withdrawal is proposer-only**, and the authorisation sits in the `Withdrawn` handler rather than in `MembershipState::authorise`: the rule is "you proposed it", which needs the pending map to check, and leaving it to `authorise`'s catch-all would have let any member withdraw anybody's proposal — handing one core member a veto over a decision a majority of the others were reaching, which is the thing the thresholds exist to prevent. **The listing is `group.pending` rather than `node.status`**, against the plan's wording: status is a summary whose every other field is one line, and a listing there would be its only unbounded one, so status carries the count and points at the listing. |

| P2-6 | §4.4 | Nothing in the design bounds how long a submitted change waits, because §4.4 does not contemplate one waiting at all — step 2 "commits" | **At most one proposal waiting per subject**, where the core group is a subject of its own, with a second refused naming the one already waiting; and **a proposal stops waiting once the log has advanced `PENDING_EXPIRY` = 128 entries past it**. `MembershipState::expires_after` and `group.pending`'s `expires_after_changes` make the countdown visible. | The three findings from reviewing 2.2-1, which are one change because each alone is worse than all three: expiry without one-per-subject still lets duplicates pile up inside the window; one-per-subject without expiry re-creates the stuck slot that made duplicates acceptable; and 2.2-2's withdrawal does nothing about a proposer who has gone away. **The core group had no subject at all**, so a pending `CoreGroupChanged` was the one kind of proposal nothing could conflict with and nothing could ever clear — a member who kept asking to join the core group made a fresh entry every time, for the life of the group. **Refused rather than superseded**, because superseding would let anybody discard the approvals a proposal had gathered by proposing again. **Counted in log entries, and this is a real limit rather than a detail.** Not because a timestamp could not be folded — `at` is signed, so every node reads the same bytes and would reach the same verdict; that argument, made when this landed, was wrong. It is that nothing *verifies* it (P1-3), and expiring on time needs a "now" the fold does not have: the only candidate is the timestamp of the entry being applied, equally self-reported, so one member with a clock a year fast would sweep the whole pending set the moment they committed anything. Not an attack under §2, a misconfiguration — and those are ordinary. Log position carries no such blast radius and compaction neither renumbers nor reuses it. So expiry measures group *activity*, not time — in a quiet group a year-old proposal may be three entries old and will still be waiting. It bounds the map, which is re-encoded into redb on every apply, and it clears a slot whose proposer has vanished; it does not make anything expire after an amount of time, and nothing available does. **The boundary is deliberate**: the sweep runs after the dispatch, so the last index at which a proposal can still be approved is exactly `proposal + PENDING_EXPIRY`, and there are tests on both sides of it — an off-by-one in a rule that silently deletes governance state does not look like an off-by-one, it looks like an approval that did nothing. Two consequences found while building it. **Expelling a core member moves the voter set while being *about* that member**, so the subject rule alone left a core group composed against the old map waiting; `enact` now clears on either route, because `CoreGroupChanged` names the whole map rather than a delta (P1-23) and approving a stale one would silently revert the change just made. And **the rule governs proposals that wait**: a core member whose change takes effect at once is never refused as a duplicate, since an immediate change has no approvals to split. The constant lives in code rather than configuration for the same reason the majority does (P2-4), and joins §5.5's policy event when that lands. **Left open:** one number for every group is wrong in opposite directions at the two ends — a busy group burns 128 entries while a deliberation is still being had, a settled group never reaches it — and it is deferred rather than tuned blind, because nobody has watched a real group's membership-event rate yet. Tracked under "Carried out of Phase 2" in the phase-2 plan. |

| P2-7 | §4.2, §7.1 | `CoreGroupChanged` had no way to be submitted by hand, and the plan named `group.propose_core` without saying what it takes | **`group.propose_core` takes a delta — one member, and where they should be reached or `null` to drop them — and assembles the whole map from the node's own projection.** `distlib core set <member> --addr ...` and `distlib core remove <member>` on the CLI. Alongside it, **a node closes every connection it holds to a core member the log has just moved**, through `AllowlistHooks::close_connections_to`. | The event carries the whole desired core group, because a node map is what openraft is handed — but building that map is a read-modify-write, and **where it happens decides whether it is safe.** Done in the API, the read and the signature are the same node's and `StaleProposal` covers the gap; a caller that assembled the map itself would sign against a `changed_at` newer than its own read, and the guard would pass while the map put back an address or a demotion the group had just decided. **The request is tagged rather than inferred.** It says `"change": "set"` with an address or `"change": "remove"`, matching the two CLI verbs. The first shape was a single `addr: Option<NodeAddr>` where `null` meant removal — one field answering two unrelated questions, *where are they* and *should they vote*, so moving a node and demoting it differed by a value rather than a word. It also needed a custom deserialiser, because serde reads a **missing** `Option` field as `None`: a forgotten `addr` would have demoted a voter, with nothing but a helper somebody had to remember to keep standing in the way. Raised in review of this PR and right — a tag makes the mistake unrepresentable instead of guarded against, and it deleted the helper. **The connection half was found by the end-to-end test and is the larger finding.** Enacting the address change was never the problem: the leader dialled the correct new address within twenty milliseconds of the event applying. It timed out anyway — every fifty-two milliseconds for thirty seconds, and it would have gone on for ever. A node that is killed sends no close frame, so its peers keep a connection that still reads as open to a socket with nothing behind it, and while they do **iroh will not reach that endpoint id at any other address**, whatever is supplied at the call site. Letting go of the connection once the move is known is the whole of the cure, and it has to go through the hooks rather than `Connections`: that map holds only what this codebase opened, and the connection that pins the path is usually iroh-gossip's, which we have no handle on. `distlib-net`'s `tests/moved.rs` pins the behaviour at the level it lives at — two endpoints, no processes — and states in its own assertion what to check before deleting the fix. Nothing tells `Connections` separately: closing is enough, since its cache already drops a connection that is no longer live, and doing both passed every test with the second line deleted. **What this does not close:** promotion. Adding a member to the core group still commits as a proposal, gathers approvals, and is refused at the last one with `PromotionUnsupported`; 2.3-2 is where that goes. |
| P2-8 | §4.2, P1-30 | A node served `distlib/raft/0` only if it had started as a voter, so the core group could never gain one: `CoreGroupChanged` refused an addition outright (`PromotionUnsupported`) | **A follower becomes a voter without restarting.** Every node's router serves the consensus protocols from startup and refuses them per connection when it has no Raft (`Seat`); a node the log promotes stops following, blanks its state machine, builds a Raft and seats it; the leader's reconciliation loop adds it as a learner (`AddNodes`) and promotes it (`AddVoterIds`) only once replication has caught up. `PromotionUnsupported` is gone. | **`alpns()` loses its `core: bool`, and that is forced rather than chosen.** A `Router`'s protocols are fixed when it spawns and `Router::shutdown` closes the *endpoint*, so a live node cannot start serving a protocol — a follower that may be promoted has to be listening before it has anything to answer with. The old guarantee (a follower did not advertise raft, so the connection failed at the handshake with "no known protocol") is genuinely weaker now: the connection opens and is closed with `NOT_A_VOTER`. That is the same refusal a demoted voter already got under P1-22, so the behaviour is not new, only the range of nodes it applies to — and it is the only answer that can change without a restart. `a_node_that_founds_nothing_serves_raft_to_nobody` was rewritten around the new shape and still checks the claim that matters: a node founding nothing accepts consensus from nobody, the real founder of a real group included. **The state-machine reset is the load-bearing step, and not for the reason first written down.** The obvious argument is "fold the log exactly once" — a follower's projection is already complete while its Raft log is empty, so replication would re-fold events it had folded. Tried: the second fold converges on the same membership anyway, because most of it is refused the second time and a refusal leaves the state alone. The real reason is reachability: a node with a founded log and no Raft voters of its own is precisely what `RaftProtocol` refuses consensus to (P1-22, the rule that stops a follower being talked into a group it is not in), so a promoted node that kept its projection would refuse the very replication meant to catch it up. Mutation-checked: skip the reset and the node takes its seat and is never made a voter. Blanking puts it back in the "no group yet" window — which is why `TrustedCore` exists: that window's stand-in used to be the configured `[consensus] core`, and for a node that joined by ticket years ago that is the wrong answer, so it is replaced with the core group the log gave in the moment before it was blanked. The allowlist survives the blanking untouched, because the allowlist task leaves the last set in place when no group is founded — the same rule that lets a founder connect before there is a log. **Two rounds, because `AddVoterIds` does not wait.** openraft promotes a lagging learner happily, which would move the quorum before there was anybody new able to contribute to it; so the loop waits for replication to reach what the leader has committed, and `Pass::Waiting` gets its own poll because a learner finishing its catch-up wakes nothing — replication progress is not on the metrics the loop watches. **Left alone:** gossip. A promoted node keeps the listening half it had as a follower rather than switching to announcing, which is a degradation and never a break — the other core nodes announce — and changing it means restructuring that task for a case that affects nothing. |
| P2-9 | §4.4, §4.5 | A pending core-group proposal was discarded whenever the core group moved at all; `reset_for_promotion` cleared the applied state but not the snapshot beside it; `MemberAdded` overwrote the whole `MemberRecord`; a demoted core node kept its Raft; `display_name` and the expulsion `reason` were unbounded | **Five review findings fixed.** A pending `CoreGroupChanged` is now compared against the change just made rather than assumed stale by it; the snapshot is cleared in the same transaction as the applied state; `MemberAdded` preserves `pledge_bytes`; a demoted node empties its seat, shuts its Raft down and starts following; and free text on a proposal is bounded at both pre-commit entry points. | From an external review of the 2.2/2.3 membership work (PR #35). Four of the five are recorded here rather than as bug fixes because each one settles a design question the original code had answered implicitly. **MEM-01 — the review's own recommended fix was not taken, and the reason is the sharper half of this entry.** It proposed pruning only when the *voter ids* move, leaving an address-only change to spare every pending map. That reopens P1-23: a map composed before a core node moved still names its old address, and `CoreGroupChanged` writes the whole map, so approving it later puts the dead address back — under `relay_mode = "disabled"`, permanently, with a majority's signature on it. Mutation-checked both ways: applying the review's narrowing fails `mem_01_a_pending_core_map_that_would_revert_an_address_is_dropped_and_can_be_remade`. **The rule taken instead is precise rather than narrow**: a pending map is superseded iff it *names* a member the change moved, at anything other than where the change put them. Absence never counts against a map, which is the whole gain — a pending demotion says nothing about where its subject is, so a move cannot answer it, and it keeps the approvals it has gathered. What this does not save is a pending promotion caught by an address change: its map genuinely does carry the stale address, so it goes. **That is a real limitation of whole-map events**, and the alternative was considered: refuse the colliding change instead, preserving the governance state at the cost of blocking the urgent address fix — and `Withdrawn` is proposer-only, so an absent proposer would deadlock it for `PENDING_EXPIRY` entries. Dropping the stale proposal never blocks and loses only something re-makeable, so that is the trade taken; every drop is now logged with the proposal's index and its proposer, so an operator knows what was lost and who to ask to remake it. **MEM-02's worse half is compaction, not staleness.** A stale snapshot handed to a leader is bad; `build_snapshot` refusing every later snapshot as older than the stored one — so the node's log never compacts again — is worse, and it is what fixing MEM-04 makes easy to reach, since demote-then-promote no longer needs a restart in between. **MEM-04's promotion is once and its demotion is once**: a demoted node's Raft log is no longer the empty one a new learner should have, so being promoted again in the same process is not a path anything here has tested, and a restart is what re-opens it. Its catch-up also waits on the 30-second idle poll, because gossip still does not change sides — the register entry from 2.3-2 stands. **MEM-06 is deliberately not in the fold**, which is the one rule in the crate that is not. Everything in `MembershipState::apply` is a rule about what the state may become and must be reached identically on every node; this is a rule about what the log should be made to carry, and by the time the fold runs every node already has the bytes. So it sits at the two doors into the log instead — `MembershipNode::propose` and the forwarded path through `MemberlogProtocol` — and a core node that skipped it could still commit an oversized entry. That is a foot-gun guard under §2's threat model, the same standing as the empty-core-group refusal. **MEM-05 was already planned as future work and is untouched.** |
| P2-10 | §8, §5.1 | §8 lists `distlib` as "the binary: CLI, config, wiring", and P1-11 had the consensus node build the router because `distlib-net` could not serve `distlib/raft/0` | **The process's iroh runtime moves above consensus.** `crates/distlib` gains a `[lib]` target exposing `Runtime`, which binds the endpoint, spawns the one `Gossip`, starts `MembershipNode` on both, and serves what the node asks for through the new `distlib_net::serve`. `MembershipNode` keeps the endpoint but no longer owns the router or the gossip instance, and declares what it needs served as `protocols()`. | **Forced by iroh-docs, not chosen for tidiness.** Docs must be handed the same `Gossip` the memberlog announces on, and docs, blobs and gossip must be accepted on one router — so a second `Gossip` or a second router is not a design option, and the thing that owns them cannot be a crate that `distlib-consensus`'s own tests depend on or the graph cycles. The binary's crate is the cheapest place that satisfies both: everything it assembles already depends on the crates below it, it changes no dependency direction, and a `[lib]` target costs one manifest section. **The shutdown order is deliberately unchanged** — node tasks, Raft, gossip task, then router, which closes the endpoint. Shutting the router first is defensible, since it stops new connections arriving mid-teardown, but it is a different choice with different failure modes and moving ownership is not the change that should make it; `Runtime::shutdown` is what the two paths that documented caring (the failed-founding path, and the expelled exit of P1-40) now call. **What the split newly risks is drift**, and it gets the test rather than a comment: the endpoint is built from `alpns()` before any handler exists — it has to be, since the node needs the endpoint — so the ALPN list and the handler list are two declarations of one fact in two crates. `a_node_serves_exactly_the_protocols_its_endpoint_advertises` compares them as sorted lists, which also catches a duplicate, because iroh's protocol map is a `BTreeMap` and a second handler for an ALPN silently replaces the first. Mutation-checked both ways: drop a handler and it fails, advertise an unserved ALPN and it fails. **`protocols()` includes the gossip handler although this crate did not author it** — what must be served and what we wrote are different questions, and only the first is the one the router and the endpoint both need answered. **`Transport` — the endpoint and the gossip over it — lives in `distlib-net`, not in the crate that first needed it.** Raised in review of this PR and right: a process's transport handles are not consensus's idea, and from 2a-2 `distlib-sync` needs the same pair. The rule it settles is that a concept shared by two subsystems belongs in the crate below both, on its first sharing rather than its second. **`Runtime` uses `anyhow`**, unlike the crates below it: it is the application's own assembly, in the crate CLAUDE.md's "anyhow for application-level" rule describes, and the `thiserror` boundary stays where the library crates are. |
| P2-11 | §5.1 | "Three namespaces (iroh-docs replicas)… namespace secrets are distributed via the join flow" | **One document per group, and its key is derived rather than distributed:** `blake3("distlib.catalogue.v1" \|\| group_id)`, which every member computes from what it already has. No event, nothing in the log, and everything — items now, ratings and works later — under key prefixes in the one document. | **The distributed version was built and thrown away, which is the useful half of this entry.** A `NamespaceCreated { kind, secret }` event, core-only, folded into `MembershipState`: PR #39, closed unmerged. It fails on its own premise. A secret that travels in the membership log is readable by exactly the people who get the write capability anyway, so it grants nothing inside the group; outside it, a non-member never gets a connection, because the allowlist refuses them at the endpoint — which is what §5.1 already says ("not the security boundary"). What remained was cost: an event, a `MembershipState` field that broke the stored format for every existing data directory, a repair path for groups founded before it, and mode 0600 on `raft.redb`. Derivation has one consequence, stated rather than discovered later: **knowing the group id is knowing the write capability**, and the group id travels in tickets. That only matters in a world where the allowlist has already failed, and it forecloses exactly one thing — handing an outsider the `NamespaceId` alone, for read-only access. If that is ever wanted, it is a *second*, random-keyed document created then; nothing here prevents it. **Three namespaces collapse to one for the same reason.** The separation §5.3 relies on is key shape (`rating/{item_id}/{member_id}` is conflict-free because each author writes only their own key), not document boundaries. Separate documents buy separate *audiences* — syncing or writing one without the other — and nothing in the plan asks for that. Splitting later is free: phase 4 chooses before it has written a single community entry, so there is nothing to migrate. **The derivation is a wire fact**, like the group id it is built on: two nodes computing it differently do not error, they open two empty documents and sync nothing. So it is domain-separated and pinned by a golden value, and the mutation that makes one node derive its own key fails the convergence test after thirty seconds of silence — which is what the bug would look like in the field. |
| P2-12 | §5.1, §8 | "`iroh-docs` as sync engine only… wrapped, not exposed" — the plan names the rule but the risk is only stated in the phase doc | **iroh-blobs 0.103 is adopted with its own quality warning, and containment is the mitigation.** `distlib-sync` is the only crate that names `iroh_docs` or `iroh_blobs`; everything above it sees `Catalogue::put`/`get` and, later, typed records. | iroh-blobs' README says in its third line that this version "is not yet considered production quality" and points at 0.35 — which predates iroh 1.0 and cannot be reached from it, while iroh-docs 0.101 hard-requires 0.103. There is no version of this phase that avoids the caveat short of hand-rolling range-based set reconciliation, which §5.1 rejected for good reason. So: take it, pin it exactly, and keep the seam sharp enough that replacing either crate replaces one layer. **`redb-v2-migration` is off deliberately** — it exists to read stores written by iroh-docs 0.94–0.98, which we have never written, and it pulls a second redb major into the graph; opening such a store without it fails loudly, naming the feature. `rpc` is off on both: it gates cross-process transport we do not use and drags in an unused TLS stack. With those off the graph has one redb, matching the `=4.2.0` already pinned. **Watch `constant_time_eq`**: iroh-blobs requires it with an upper bound only, in a workspace that `=`-pins everything, so `cargo update` can move it. **One builder deliberately not used:** `Docs::persistent` pairs the replica store with `DefaultAuthorStorage::Persistent`, a `default-author` file that is a second copy of this node's identity and hard-errors when the two disagree. The author *is* the node key (§5.3's per-author keys then need no mapping, since `AuthorId == MemberId` by construction), derived every start, so there is nothing to persist — `Engine::spawn` is called directly with `DefaultAuthorStorage::Mem`. The cost of that identity, stated: one ed25519 key signs both iroh's TLS handshakes and docs entries. They are domain-separated by protocol and neither is an oracle for the other. |
| P2-13 | §5.2 | Field-level keys with mixed value encodings ("string", "JSON array", "int"), and four keys recording who changed what and when | **The §5.2 schema, in `distlib-core`, with every value JSON** — a title is `"Dune"`, quotes included. `created`, `last_modified`, `modified_by` and `added_by` are **not** keys, as D3 said: they are derived from iroh-docs' own entry timestamp and author. Reading is total: a key this build does not know, and a value it cannot read, are answers rather than errors. | **One encoding beats a table of exceptions.** The reader has a single code path, and a `Dune` that arrives where `"Dune"` was expected is a malformed value rather than a second format to support — which matters because the catalogue is one document that will outlive this build's idea of what is in it. **Reading has to survive the future**, so `Item::absorb` answers `Took`, `NotThisItem`, `Unknown` or `Unreadable`, and only the first changes anything: phase 4's ratings land in this same document (P2-11), a field added next year will reach a node running today's build, and neither may break reading an item. A value that will not parse **leaves the field as it was** rather than clearing it — forgetting what we could read because somebody wrote something we could not is the worse of the two failures. **`Item::entries` emits only the fields that are set**, which is not an optimisation: last-writer-wins is per key, so writing `title: null` for an absent title would erase a title another member had just filled in. An item with one field set is a one-key update, and that is pinned by its own test rather than by the round-trip, which cannot see the difference. **The fingerprint's filter lives on `Item`** — `role: content` files only, so adding a cover does not make a different item (§5.2, as amended by P0-7) — next to the type that knows what a role is, rather than at whichever call site needs an id. An item with no content files has *no* fingerprint, because one derived from an empty set would be the same id for every such item. **What we learned about iroh-docs, the hard way:** a document's live updates travel over the process's **gossip swarm**, not over the docs connection. A node that does not serve `iroh-gossip` still syncs once — the reconciliation two peers do when they start — and then never hears another word, silently, with no error on either side. That is exactly what the first version of the sync crate's test harness did, and it passed, because its single write happened during that first reconciliation. The harness now serves gossip as a real process does, and the test writes a **second** time after the first has crossed, so the live path is pinned rather than assumed. |

| P2-14 | Phase 2 plan, §5.1 | `AddressBook` must be widened in 2a-2 to learn every member's address, because otherwise a **follower-to-follower catalogue sync breaks** under `relay_mode = "disabled"` | **The premise is half right and the predicted symptom is wrong, so nothing was widened.** Two followers with relays disabled *do* converge — that acceptance passes — because the core node relays. What does not work is the thing underneath it: a follower can never resolve another follower. The widening is deferred with the failure recorded as an ignored test rather than a sentence. | **Verified before being fixed, as the phase doc instructs, and the verification changed the answer.** With the only core node up and the catalogue converging normally, `carol.endpoint().connect(bob_id, PING)` answers `No addressing information available`. So `what_one_follower_writes_the_other_follower_reads` passes on alice relaying, not on bob and carol ever speaking; stop alice and nothing crosses, at 150 s as much as at 30 s. **Not a localhost artefact**: `endpoint().addr()` carries a usable direct address on every node in the test, so gossip has something real to publish. The cause is that `GossipAddressLookup` — which iroh-gossip *does* install on our shared endpoint, so the premise that it would serve docs' downloader was right — is filled only from the peer data on **Join and ForwardJoin** messages, and two followers that joined through the same core node never learn each other that way. `distlib-net`'s `addresses` module claimed the opposite and is corrected in this PR; that claim was reasoning, this is a measurement. **Why deferring is defensible rather than lazy:** fixing it needs every member's address announced somewhere, since a `MemberRecord` carries none and Raft's node map holds voters only — a new channel, in a sub-phase whose stated acceptance already passes. And a group whose core nodes are all unreachable cannot fetch the membership log either, so the catalogue is not the first thing to stop working. The consequence to state plainly: **a follower reaches the group through core nodes**, which is a property of the design that nobody chose. The gap is pinned by `a_follower_cannot_resolve_another_follower`, which passes today; **the acceptance for the widening is that test failing**, and its failure message says so. **Closed by P2-15**, in the PR after this one. |

| P2-15 | §5.1, and P2-14 above | Nothing: a follower's address is recorded nowhere, so the catalogue passes through a core node | **Every member announces where it is**, signed by its own key, on the group's existing gossip topic; a second lookup on the endpoint holds what they say; and the catalogue syncs with the whole membership rather than the core group. A follower now resolves a follower, and two followers reconverge with the core node stopped. | **Signed, unlike the log announcement beside it, because the cheap check is not available.** iroh-gossip's `Message::delivered_from` is documented as "not the same as the original author" — in an epidemic broadcast most messages arrive relayed — so an address cannot be attributed by who handed it over. It is also the thing other nodes will *dial*, where lying about the log costs one wasted fetch. Signing also means a core node can serve these on without being trusted not to forge them. **Freshness is the announcer's applied log position, not a clock.** A wall-clock stamp fails without an adversary: a clock that jumps backwards (restored VM, NTP correction, no RTC) makes a node's newest statement sort older than the one its peers hold, so it is discarded and the node stays unreachable at its new address. A per-member sequence number has the same bug, since it resets on restart. The log position only advances, survives restarts, and is the one number every member agrees the meaning of. It gives *less* than a total order — two statements from one member inside a single stretch of unchanging membership tie, and the later arrival wins — which is stated where it is defined rather than discovered later. **The directory replaces where `AddressBook` merges**, because `MemoryLookup` merges and a laptop moving between networks would otherwise have every address it ever had raced on every dial. **Three triggers, no timer, and the obvious one was wrong.** Re-announcing when a neighbour arrives sounds like the answer to a late joiner and is not: two members who cannot yet resolve each other never *become* neighbours, so in a star-shaped swarm nobody downstream sees the newcomer at all. Deleting that trigger broke no test, so it is not in the code. What carries a late joiner is **hearing where somebody new is** — somebody this node has only just heard of has probably not heard of this node either — which is symmetric and pinned by a test that fails without it. **Symmetric means it has to be shown to stop**, and the thing that stops it is that the directory reports a *change* rather than an acceptance: a member restating an address we already hold wakes nothing. That check was added after the fact, and the reason it was not missed is worth recording, because it is the sort of thing that gets built on by accident. iroh-gossip discards a message whose id it has already seen, and a repeat is byte-identical — the signature is deterministic and the payload carries no clock — so the cascade was dying in the broadcast layer, on a ninety-second cache whose retention is a default in somebody else's crate. It was not free even there: the duplicate still crossed the wire to every neighbour, each of which answers `Prune` and moves the sender to its lazy set, so the *next* real announcement reaches them the slow way. **What this forecloses, deliberately:** nothing that varies may enter `SignedAddress`. A timestamp or a nonce would make every repeat a distinct message, and `a_settled_group_stops_talking_about_addresses` is the test that says so — measured, by signing each announcement one position further along, which leaves three nodes still announcing with nothing happening. **And the peer set is not a one-off**, which cost a day: iroh-docs offers a document's peers to gossip once and never retries one it could not resolve, so every follower named at open time was named and dropped. The peer is offered again when its address is learned, and the whole set again slowly regardless — because the node that introduced two members can leave afterwards, and gossip does not repair a swarm it has lost its last neighbour in. **Those are two different rounds on purpose**, and the reason is that `start_sync` is not an add: it dials every peer in the list it is handed, new or not, and appends up to five of the document's own remembered peers on top. So a reaction hands it only what was learned, and only a repair — a membership change, or the timer — hands it everything. The timer is measured from the last full offer rather than from the last wake-up, which matters only once the two differ: a group whose members keep moving would otherwise restart it for ever and never sweep. Measured: without that timer, two followers never reconverge after the core node stops, at 90s as much as at 30s. **The exposure this accepts, decided rather than assumed:** every member now learns every member's address, where before a follower's was known only to core nodes. Agreed as consistent with §2's closed, trusted group. |

| P2-16 | §5.2 | An entry and its content replicate together: iroh-docs is the whole of catalogue replication, so an entry that has arrived is an entry that can be read | **A node asks the group for content it holds the key to and not the bytes**, on a five-second sweep of its own document, and the flaky test that was blamed on addressing turns out to be this. | **iroh-docs fetches content when an entry *arrives* and at no other time, and the arrival is only credited with a provider if it came straight from the publisher** — `engine/gossip.rs` reads `msg.scope.is_direct()` and records `ContentStatus::Missing` for anything relayed. A relayed entry therefore starts no download and remembers no provider. What is meant to rescue it is `Op::ContentReady`, broadcast when a node's own download finishes: **once**, to **direct neighbours only**, and never by the author, whose local write emits `Op::Put` and nothing else. A node that is not a neighbour of whoever finishes first, at the moment they finish, keeps the key and never gets the value. Nor does anything retry — a failed download goes back into `missing_hashes` with no timer, and the engine's provider list is a snapshot taken when the download started, so a peer learned afterwards is never tried. Re-offering peers does not help either: the entry is already held, so reconciliation produces no insert. **Measured, not inferred:** `what one follower writes the other follower reads` fails about one run in ten under `taskset -c 0,1`, always as *the entry is here, its content has not arrived* — and at the same rate on the commit before P2-15's addressing work, which is what withdrew the first explanation. **Why it is repaired here and not upstream:** the fix needs a provider list, and this is the layer that has one — the group's own membership, filtered to the members this node can reach, which is the same set the document is offered as peers. **And it only works because of P2-15.** The downloader is handed bare endpoint ids and has to resolve them; before members announced where they are, this retry would have failed exactly the way the download it repairs did. **Pinned deterministically rather than by the flake**, because provoking a relayed entry means dictating a gossip topology and no test can: a node is restarted holding its document store and an empty content store, which is the same dead end reached by ordinary means — and is itself a real case, since an in-memory content store on a node that restarts is exactly that. Without the sweep that test times out at 30 s; with it, it reads in about five. **What this accepts:** there is no backoff, so a node left holding content nobody reachable will serve — an expelled member, say — asks again every five seconds for as long as it runs. Recorded as open rather than solved, on the P1-35 precedent: the cost is one round of dials on a timer, and what the backoff should be is not knowable from an argument. |
| P2-17 | Phase 2 plan, 2a-5 | Addresses live in memory only, and that is safe because "a restarted core node's directory refills from the announcements its reappearance provokes" | **A core node answers `Request::Directory` on `distlib/memberlog/0` with every statement it has heard**, and a follower asks once, after its first answer about the log. **The quoted justification is withdrawn in this PR**: it stopped being true when P2-15 made `Directory::learn` report a *change*. | **The refill path was closed by the previous PR, and nobody noticed.** A node that restarts at an unchanged address announces; its peers already hold that address, so `learn` answers `false`, nothing bumps `learned`, and the announce loop — whose only triggers are this endpoint's address changing and `learned` bumping — never fires. It hears nothing back. Before change detection an equal position tied and counted as news, so its peers *did* re-announce and it *did* refill. So this sub-phase is no longer a convenience for late joiners; it is the only recovery path a node has for what it did not hear. **Asking a core node places no new trust in it.** What comes back is the members' own signed statements, and the asker verifies each one — the relay cannot forge an entry, and the design never has to assume it would not. **One bad entry costs only itself**: a forgery slipped into an honest batch would, under a loop that stopped at the first bad signature, cost the asker every address behind it, so each is judged alone. Unit-tested rather than end to end, because an honest core node cannot be made to hold a forgery — the same verification runs on the way in. **Expelled members are filtered at serve time, not forgotten.** The directory records what was *said* and the log records who is *admitted*; letting the second edit the first would put an eviction race inside a type whose whole job is to record statements. Read at the moment of answering, so it is current by construction. It buys little in itself — being resolvable was never admission, and `AllowlistHooks` refuses a non-member either way — but a core node should not hand out an address the group has voted to be rid of. **Unpaged, unlike the log fetch**, and the arithmetic is worth writing down rather than reaching for a cursor: this is one statement per *member*, not per log entry, so it is bounded by the group's size and not its age. A statement measures 139 bytes with one direct socket and no relay url — measured, and held under 320 by a unit test so a real one has room for both — at which `MAX_RPC_BYTES` is reached somewhere past fifty thousand members, an order beyond the thousands §2 allows for. **`UpToDate` triggers the ask as well as `Fetched`**, which is not a detail: a node whose log is already current never sees `Fetched`, and a restart is the case this matters most for. **Measured:** the acceptance — bob announces, bob stops, carol starts and still resolves him — passes in six to thirteen seconds and fails on the timeout at forty-seven with the ask removed. Bob is stopped on purpose; left running he announces the moment he hears of carol, gossip alone passes, and the test would prove nothing. **Two things left open.** The sub-phase also specifies asking "whenever a member cannot be resolved": nothing detects that in one place today, and wiring dial failures is a separate concern. And **a restarted core node still cannot refill** — `follow` and `core_group::enact` are exclusive, so the ask never runs on one. Both are recorded in the phase document rather than guessed at. |
| P2-18 | P2-16, and the open item it left | The content sweep has no backoff, so a node holding an entry whose bytes no reachable member has "dials the group for it every five seconds for as long as it runs" | **A hash is asked about once per generation**, and a generation ends when the membership changes or after five minutes. **The quoted description was wrong**, measured rather than re-reasoned, and what it was hiding was worse than what it described. | **Measured first, and it changed the fix.** Two nodes hold the same entries and neither has the bytes, so every sweep asks a reachable peer that cannot answer. Time from the start of one round to the next: **6.0 s at one stranded hash, 10.0 s at five, 25.1 s at twenty** — `5s + K × 1.0s`. A download that fails against members who are reachable and simply lack the bytes costs about a second, so `FETCH_DEADLINE` never came into it: the loop was paced by how much it had already failed at. So there *was* a backoff, arrived at by accident, and it had the worst shape available — **the more is stranded, the longer a newly stranded entry waits behind it to be asked even once.** At twenty hashes that queue is already twenty seconds and it grows with the document. That is the part the open item did not describe, and it is the part worth fixing; the dialing was only the part that was noticed. **One rule, not a schedule.** A set of what has been asked about, pruned to the want set every round so it cannot outlive what it is about, cleared when a generation ends. The generation ends on a **membership change** — a member who has just joined is the likeliest holder of bytes nobody here had — and otherwise on a five-minute timer, which is the backstop for the other way the answer can arrive: a member who was already present finishing their own download, which nothing tells this node about, since `Op::ContentReady` reaches direct neighbours once and that is the very gap this sweep papers over. The loop now **selects on the membership channel** instead of reading its latest value and never waking; without that the reset cannot fire and the sweep would be one that backed off from its own answer. **Measured after:** the same twenty stranded hashes cost one round of 25.1 s and then rounds of **5.00 s that ask for nothing at all**, until the group changes or the timer runs out. **Why not per-hash backoff**, which is the obvious alternative: it needs a counter and a deadline per hash and a schedule to pick, and the precedent (P1-35, `PENDING_EXPIRY`, the O(N²) offer) is not to pick a schedule from an argument. One set and one reset rule does the measured work. **Why not stop scanning and subscribe to the document instead**, which is the other obvious alternative and is now foreclosed rather than merely unchosen: `Doc::subscribe` is an `async_channel::bounded` that the engine writes with `sender.send(event).await` — it **blocks the live actor** rather than dropping, so a subscriber that does anything slow, such as a download, would stall iroh-docs' own sync. **What is still open:** a node with more stranded hashes than a generation has seconds is busy most of the time, because asking each once is the floor that repair can cost. And the scan itself is still O(entries) plus a store call per unique hash every five seconds — `blobs.list()` would collapse those into one call and must not be used for it, because it returns partial blobs where `status()` distinguishes `Complete`, so a half-downloaded blob would count as present and never be asked for again. Both are rows in the phase document. |
| P2-19 | §5.4, and 2a-3 in the phase plan | "A projection task consumes the iroh-docs event stream (plus membership log events) and upserts into SQLite tables", and "cold-start = full docs replay; projection is idempotent" | **The stream carries item ids, not entries, and the projection re-reads rather than upserting what the event said.** A change marks an item dirty; the projection then asks the document for that whole item and overwrites its rows. **The replay is not only for cold start** — it runs at every start. **Split: 2a-3a is this; tantivy and `admin.reindex` are 2a-3b.** | **Re-reading is what makes idempotence structural rather than careful.** "Replay N times = replay once" is a property to be argued about when a projection applies deltas, and a tautology when each unit of work is a whole-item overwrite derived from the document — there is no accumulated state to replay into. The same move makes order irrelevant, which is the stronger property and the one the proptests pin: order independence is what stops holding first if somebody later optimises the re-read into an update. **Entries could not have been carried anyway.** An entry's value is a blob that may not have arrived, so a stream carrying values either blocks or lies; and `Doc::subscribe` is an `async_channel::bounded` written with `send().await` (P2-18), so the subscriber **stalls iroh-docs' live actor** if it does anything slow. The pump therefore parses a key, touches a set and returns; every slow thing happens on the reader's side. The set also coalesces, so six field writes to one item are one re-read. **`ContentReady` carries a hash, not an item**, and is handled by keeping the set of items last projected *incomplete* rather than an index of which items await which hashes: derived rather than maintained, empty in the steady state, and nothing to rebuild after a restart. That set also covers content the projection is never told about, which is not obvious and was checked rather than assumed: `ContentReady` is emitted only from the live actor's `on_download_ready`, so bytes reaching the store by any other route emit nothing — and the catalogue's own repair sweep (P2-18) is exactly such a route, since it drives the blobs `Downloader` directly for content the docs engine gave up on. `PendingContentReady` closes it, and not by luck: it fires on **every** sync round that finishes with nothing queued, and the catalogue re-offers its peers on a 15-second timer whether or not anything changed. **Measured on a quiet two-node group: nudges at 0.03 s, 14.9 s, 29.9 s, 44.9 s**, so an item repaired behind the engine's back is re-read within about fifteen seconds. A node with no reachable peer gets no rounds and no nudge, which costs nothing — with no peer there is nothing for the sweep to fetch either. **The blink is accepted, deliberately.** An item whose newest `title` entry has arrived but whose bytes have not reads with no title, so projecting it writes `NULL` over a searchable title until the bytes land. Keeping the old value was rejected because it makes the tables a function of *what this node happened to see* — which is exactly what 2a-3's acceptance forbids: a node that restarted would then hold different rows from one that did not, with no repair path. A gap measured in seconds beats a divergence with none. **Replay at every start, not only a cold one**, because a start and a reindex are then the same operation and there is one of them to keep true. It costs one read and one upsert per item per start. **The acceptance needed two tests, not one.** "Kill and restart; SQLite matches a node that never restarted" passes with the replay removed — the database is still on disk and the few entries missed while down arrive as live changes. Deleting `db/` before the restart is what the replay alone can fix, and that is the mutation pair: with the replay disabled, `a_read_model_that_was_lost_is_rebuilt_from_the_document` fails and `a_restarted_node_holds_what_a_node_that_never_restarted_holds` still passes. Both were run. **A third mutation was run for the thing two agreeing nodes cannot catch**: "bob matches alice" compares two projections of the same code, so a field silently dropped on the way out of the catalogue is dropped on both and they agree about it. Dropping `genres` in the projection leaves every `agree_on` passing and fails only the assertion against a written-down item, which is why that assertion is there. **`rusqlite` with `bundled` and `fallible_uint`**: bundled because `STRICT` tables need SQLite 3.37 and the schema should be checked against one known version rather than the host's; `fallible_uint` because SQLite's only integer is signed and a `u64` size must be refused past `i64::MAX` rather than wrapped. **Columns §5.2 names and this does not have**: `added_by`, `created`, `modified_by` — the catalogue's `fields!` table has no such fields, so each would be `NULL` in every row of every group. `last_modified` is the exception precisely because it needs no field: the document already timestamps every entry. |
| P2-20 | §5.4, and 2a-3b in the phase plan | "tantivy indexes `title, authors, description, genres, series, reviews` with per-field boosts"; 2a-3b adds "tantivy over `title, authors, description, genres, series`... plus `admin.reindex`" | **Built as scoped** — `reviews` left out with the same argument schema.rs makes for the SQL tables it does not have yet — **but the boost numbers are a guess, not a measurement**: nobody has run a query against a real catalogue, so `distlib-store::index` picks title > authors ≈ series > genres > description on intuition alone and says so in the constant's own doc comment. `admin.reindex` reaches `distlib-api` and the CLI in this PR; `library.search`/`library.item` stay 2a-4's, per `distlib-plan.md`'s own phase-2 checklist, which already calls the store's half "search API (internal)". | **Two real bugs, both found by a test rather than by inspection.** First: `IndexReader`'s default `ReloadPolicy::OnCommitWithDelay` does not make a commit's writes visible to a search that runs right after it returns — it schedules a background reload on its own timer, "within a delay measured in milliseconds" by tantivy's own docs — which is silently wrong for a method named `commit` on a type whose whole contract is "written, then visible". Fixed by opening the reader with `ReloadPolicy::Manual` and calling `reader.reload()` synchronously inside `commit`, so there is one, deterministic answer to "when is a write visible" instead of two racing ones. Second, and the one that cost the most time: a same-process restart raced `LockBusy` on reopening `index/`, caught by the acceptance test in `distlib/tests/read_model.rs`. Tried first, and rejected: replacing `Projection`'s plain `task.abort()` with a cooperative stop (`tokio::sync::Notify` plus a `watch::channel(bool)` the task flips before returning) — it fixed nothing, because the real cause was one layer down. **tantivy's own `IndexWriter::Drop` signals its merge thread to stop but does not join it** — only the consuming `wait_merging_threads(self)` does that — so the writer lockfile could still be held after every reference to the `IndexWriter` was gone, regardless of how cleanly the task around it shut down. `Projection::shutdown` is back to plain `abort()`; the fix instead changed `SearchIndex`'s writer to `Arc<Mutex<Option<IndexWriter>>>` and added `SearchIndex::close`, which takes it out of the `Option` and calls `wait_merging_threads` on it. `Runtime::shutdown` calls `search.close().await` explicitly, after `projection.shutdown()` and before the catalogue closes under it. Caught by, and pinned by, `a_search_index_that_was_lost_is_rebuilt_from_the_document` — the 2a-3b analogue of P2-19's restart acceptance, deleting `index/` alone rather than `db/`. **Two smaller gaps, both found the same way.** `Projection::run` awaited `catalogue.ready()` before it read the reindex channel at all, so `admin.reindex` against a node that has not founded or joined a group was not an error — it was a request that never returned; a node in that state now answers a reindex at once, since an empty catalogue reindexes to nothing (`admin_reindex_does_not_hang_before_a_group_exists`). And `TopDocs::with_limit(0)` panics rather than returning nothing, in tantivy 0.26.2 — `SearchIndex::search` answers `limit: 0` without asking tantivy at all (`a_zero_limit_finds_nothing_without_panicking`). **The boost-ordering test depends on an undocumented tie-break.** `a_title_match_outranks_a_description_match` gives both fields the query term at equal length, which tantivy scores as an exact tie absent a boost; the test relies on that tie breaking by insertion order rather than on any guarantee tantivy documents. Safe under the `=0.26.2` pin, but the mutation this test is built on — every boost constant set to `1.0`, expecting `by_description` first — should be re-run by hand on any future tantivy version bump, since a changed tie-break would silently turn it into a test that always passes. |

| P2-21 | §7.1, and 2a-4 in the phase plan | `library.search {query, filters{type,genre,lang,author,series}, page}`; `library.item {item_id} → full record incl. ratings summary, files, availability`; 2a-4's checklist: "`library.search` and `library.item` as JSON-RPC methods (§7.1 names verbatim)... reading SQLite/tantivy only" | **The two method names, verbatim, and nothing else from the sketch verbatim** — the plan calls the whole `library.*` group a sketch for "Claude Code to flesh out request/response types", and three things named in it are not here. **No `filters`**: `authors`, `genres` and `series` are already free-text fields a query can point at directly (`authors:herbert` is valid tantivy syntax today, checked by hand against a running node — the doc comment first written here said `author:herbert`, singular, and was wrong; the by-hand check is what caught it), but `kind` and `lang` are SQLite-only columns tantivy never indexes at all, so a `filters` object would either duplicate the first case or paper over the second. **No `page`**: `limit` is flat, following this file's existing param style (`Proposal`, `PledgeSet`) rather than introducing this crate's first nested object; there is no `offset`, though tantivy supports one natively (`TopDocs::and_offset`) — adding it means extending `SearchIndex::search`'s signature and every existing caller for a consumer that does not exist yet. **No ratings or availability in `library.item`**: nothing populates either, the same reasoning `schema.rs` already gives for leaving the `reviews` table out of `distlib-store` — phase 4 and 5 own them. `library.search` returns a summary (`item_id, kind, title, authors, genres, series, year`) rather than the full record; `library.item` is where files, description, language and `last_modified` are read. `limit` is capped at 500 regardless of what is asked for — loopback and token-gated, so this guards a mistake rather than an attacker, but `TopDocs::with_limit` allocates a heap sized to it and no caller of a personal library needs more in one page. | **The phase's own acceptance — "by hand: two nodes, add metadata on one, search by author on the other" — could not be run as written**, because nothing writes catalogue metadata yet: `library.add` is not 2a-4's, and there is no other path, CLI or RPC, that creates or edits an item. What stands in for it: `library_search_and_item_read_what_the_catalogue_wrote` in `distlib/tests/read_model.rs` writes an item through a real `Catalogue`, waits for the projection to make it searchable exactly as the two-node convergence tests already do, and then calls `library.search` and `library.item` through the real `Api::call` dispatch rather than `Store`/`SearchIndex` directly — closing the one link 2a-3b's own tests did not reach: that `library.*` actually reads what the projection wrote. The `distlib-api` unit tests (`library_item_reads_the_stored_record`, `library_search_ranks_and_reads_hits_back`, and their two refusal cases) seed `Store`/`SearchIndex` directly instead, over real HTTP, pinning the dispatch and error-code choices (a malformed query is `-32602`; a missing item or projection is `-32000`) independently of the projection. **Nothing tests `distlib search`/`distlib item`'s own rendering** — the `authors:herbert` mistake above was in that layer, in a doc comment, and only the by-hand run against a real `distlib run` process caught it; there is no precedent elsewhere in this codebase for testing a CLI command's stdout, so this does not add one, but the gap is real and worth naming rather than leaving silent. `library.search` reads each hit's fields with a new `Store::item_fields`, not `Store::item` — the latter also fetches every file row, which a search summary never shows, so `library.item` keeps that cost and `library.search` does not pay it per hit. |
| P2-22 | 2b-1 in the phase plan | "`distlib-net::blobs`: the `BlobsProtocol` on the router, and fetch by hash from a set of member providers — **our** path, `Store::downloader` driven by a provider list we choose" | **Fetch only. The `BlobsProtocol` registration is not here** — it already exists, on `distlib_sync::Catalogue::protocols()` since 2a-2, serving the very same store this crate's fetch reads from and writes into. One ALPN has one owner (`router.rs`'s own rule), so a second registration in `distlib-net` for the identical store would either silently lose to Catalogue's in the router's fold or be redundant with it — either way, two places claiming ownership of one thing where the design this crate's own docs describe says there must be one. Raised with Ivan before writing any of this, and the smaller-scope reading was chosen deliberately: relocating the registration would mean changing `Catalogue::start`'s signature and its `protocols()` contract, a working, already-reviewed design 2a-2 set up with its own reasoning about ALPN ownership, for a change the acceptance criterion does not actually need — it tests that a fetch goes through `distlib-net`'s own call rather than iroh-docs' internal downloader, not who registers the ALPN. `distlib_net::Blobs` is therefore built as: wraps a `Downloader` (built once, per `Store::downloader`'s own advice against building one ad hoc), and `fetch(hash, providers)` asks a chosen `Vec<EndpointId>` for a hash, in a random order (`Shuffled`, the same primitive `distlib-sync`'s own repair sweep already uses for exactly this). No protocol registration, no `Router` contribution, no change to `Runtime`'s composed `protocols()` chain — that already carries `iroh_blobs::ALPN` today, unchanged. `Blobs::fetch` also carries no deadline of its own, on purpose: how long is worth waiting for a media file is a caller's question, not a fetch primitive's, and `distlib-sync`'s own repair sweep already wraps the same downloader in its own timeout rather than one baked into iroh-blobs' API — this follows that precedent rather than inventing a `distlib-net`-level constant nothing asked for. | Pinned by `crates/distlib-net/tests/blobs.rs`, at the level `moved.rs` already pins its own behaviour at: two bare `iroh::Endpoint`s, `relay_mode = "disabled"`, no consensus, no gossip, no docs. **The test adds its blob directly to a store**, never as a docs entry, because 2a-2 already moved blobs between two nodes and reusing that path would prove nothing new — the phase text says as much, and the mutation check in the test's own doc comment (unspawn the serving router; the fetch now fails with `NetError::Fetch` instead of passing some other way) is there because a positive-only test cannot rule out having accidentally exercised a shortcut. **"No address supplied at the call site" is `distlib_net::AddressBook`, not a raw `EndpointAddr` handed to `fetch`** — the fetching node installs a book on its own endpoint and learns the provider's address there, the same mechanism production already gives every protocol this crate does not author (iroh-gossip, iroh-docs) for dialling a bare id; the call to `Blobs::fetch` itself takes only `EndpointId`s. A second test pins the other side of the no-deadline decision: a provider nobody wrote an address down for is a fetch that fails within a caller's own timeout, not one that hangs — checked directly, since a `Vec<EndpointId>` iroh cannot resolve at all is exactly what an item whose only custodians are offline looks like. |
| P2-23 | §5.2, §6.1, and 2b-2 in the phase plan | "Hash the file set → blobs → `ItemId::from_content_hashes` over the `role: content` files only (D4) → catalogue entry. Exact-dup guard: compute the fingerprint before creating, and on a hit report the existing item and offer to contribute missing files rather than creating a second one (§6.1)." | **Built as scoped, with one deliberate narrowing.** `library.add` and `distlib add` only ever write `role: content` files — every path given is hashed as content and takes part in the fingerprint. Covers, subtitles and the rest of `FileRole` are left out, on the same argument P2-21 already made for `filters` and paging: nothing calls this with that shape yet, and a caller that needs to attach a cover to an existing item can write straight to `item/{id}/file/{blob_hash}` once it exists. `format` is the file's extension, lower-cased, rather than sniffed content — an operator naming `book.epub` is not a case worth a parsing dependency over, and the field is free text anyway. **The dedup guard runs before any write**: the fingerprint is computable from the hashes alone, so `library.add` asks `Catalogue::item(id)` before deciding what to write, not after. A hit **never rewrites metadata** — `kind`, `title` and the rest are left exactly as the existing item has them — and only the files given that the existing item's `files` map does not already hold are written. A miss creates the item with every field the call was given. | **The guard is not what makes the acceptance's convergence hold — §6.1 says so and it is worth restating, since the two read as one feature.** `item_id` is a pure function of the sorted content hashes (§5.2), computed before either caller touches the catalogue, so two nodes independently adding the identical file set arrive at the same id regardless of what the guard does; that is `crates/distlib/tests/library.rs`'s `adding_the_same_files_twice_from_different_nodes_converges_on_one_item`, run with `tokio::join!` so neither call can be informed by the other. **What the guard actually protects is metadata quality and a partial write**, and the second test pins both at once: an item is seeded with only one of the two file entries its own fingerprint implies — standing in for a write interrupted partway through `Catalogue::write`'s per-key loop, which is a real and recoverable failure mode, not a hypothetical — and a second `library.add` naming a worse title is checked to leave the stored "Dune" alone while still contributing the missing second file. Mutation-checked twice: removing the `!existing.files.contains_key(hash)` filter makes the test fail on the contributed-files count, and forcing every call through the "create" branch regardless of `existing` makes it fail on `created` and would fail on `title` too, had the first assertion not already caught it. **`Api` gains a `catalogue: distlib_sync::Catalogue` field** — `library.search`/`library.item` never needed one, since both only ever read the projection `distlib-store` keeps, but a write has to reach the document itself. This is a new dependency edge, `distlib-api` → `distlib-sync`, and it touches `distlib-api/tests/rpc.rs`'s `Harness`, whose own doc comment says "no catalogue, no read model" by design: both of its builders now start a lightweight in-memory (`MemStore`) `Catalogue` alongside the bare consensus node they already built, purely so `Api`'s struct literal has something to put in the field — nothing in that file calls `library.add`, and the acceptance for it lives entirely in `crates/distlib/tests/library.rs` against real, disk-backed catalogues. `distlib-sync::Catalogue` gains `add_file`, which hashes and copies a local path into the *same* store `BlobsProtocol` already serves, without writing any entry for it — kept inside `distlib-sync` rather than exposing the blob store itself, which is what keeps §5.1's "wrap, don't expose" rule intact: `distlib-api` never gains an `iroh-blobs` dependency to call this. A new `SyncError::LocalFile` variant distinguishes "this path could not be read" (the caller's mistake, `-32602`) from `SyncError::Content` (the blob store itself failing, `-32000`), which `Api::hash_file` maps accordingly. **Found while smoke-testing the CLI by hand, and left unfixed as out of scope**: `config.toml`'s own generated comment says "Leave it empty to found a group with this node alone", but `distlib run --found-group` against an empty `[consensus] core` fails with `this node is not part of the core group` — `MembershipNode::start` decides `is_core` from the *configured* core list before there is a group, and an empty list never contains this node regardless of what `commands::found` later assembles for the founding event itself. Every existing test harness sidesteps this by always passing the node's own id in `core`, which is why it was never caught. Not touched here: it is a pre-existing mismatch between a doc comment and `distlib-consensus`, unrelated to this PR's diff, and fixing it deserves its own look at whether the comment or the check is wrong. **Caught by review, fixed before merge:** the id was originally computed from a `Vec<[u8; 32]>` built one entry per path given, which drifts from `Item::fingerprint()` — the canonical computation, already relied on as an invariant by `distlib-sync/tests/converge.rs`'s `it is what it contains` — the moment two paths hash to the same content, since `files` is keyed by hash and silently collapses to one entry while the parallel `Vec` does not. Fixed at the domain level rather than by deduplicating the `Vec` in `distlib-api`: `Api::add` now builds the item's `files` map first and reads `item.fingerprint()` off it, so there is exactly one place that turns a set of hashes into an id, and `library.add`'s id can no longer disagree with what the stored item's own fingerprint says about itself. Pinned by `adding_two_paths_with_identical_bytes_keeps_the_id_and_the_items_own_fingerprint_in_step`, mutation-checked against the original `Vec`-based computation. |
| P2-24 | §7.1, §5.6, §6.3, and 2b-3 in the phase plan | "`library.download` + the phase acceptance. Provider selection from the availability we have (members holding the blob; §5.6's gossip index is phase 4, so v1 asks the members it knows), fetch, verify (inherent to BLAKE3 transfer), export, register as a holder. `distlib download`." §7.1's own sketch of the method is `library.download {item_id, file_index?} → task_id`. | **The method and the CLI verb are built; the phase acceptance is split out into 2b-3b.** `library.download` takes `{item_id, dest, file?}` and returns when the files are on disk. Four deliberate departures from the sketch, all of the shape P2-21 and P2-23 already established. **Synchronous, not a `task_id`** — a task id is only useful beside §7.2's `download.progress` SSE events, which are phase 3, so it would be a handle to nothing and a caller would have to poll for a completion that has no representation either; returning when the bytes are written says the same thing with nothing to build first, and adding a task later is an added field rather than a changed contract. **`file` is a content hash, not §7.1's `file_index`** — an item's files are a `BTreeMap<ContentHash, FileRecord>` (§5.2), so the only index available is a position in hash order, which is meaningless to a human and silently renumbers the moment another member contributes a missing file; the hash is what `library.item` already prints and what the record is keyed by. **`dest` is required and must already exist** — the listener is loopback and token-gated (§7.1, P1-25), so the caller's machine is the node's, but not its working directory; `distlib download` defaults it to `.` and canonicalises before sending, the same thing `distlib add` already does with its file paths. A download **overwrites nothing**, by three refusals rather than one: a file already at the target (the exported file is the operator's and may have been edited, so deleting it is an instruction and overwriting it would be a guess), two of the item's *own* files resolving to one path, and a `filename` that is not a plain filename. The second was caught in review and is ordinary rather than exotic — `library.add` takes a file's name from the path it was given, so a two-disc set is one item with two hashes both called `track01.mp3`, and without the check the second export silently wrote over the first while the answer reported two files at one path. **Ivan's follow-up review moved the primary check to where the names are chosen**: `library.add` now refuses to *create* such an item, because the person running `distlib add` is the one who can rename a file and they are still in the room, whereas refusing at download time tells somebody else, later, that an item they did not create cannot be written into one directory. The download-side check stays as the second line and is not redundant — the catalogue is one document for the whole group, so an item's files can be written by a member on an older build or by a future one filling the roles `library.add` does not write yet, and a method about to write files to a disk checks what it was handed rather than trusting who handed it over. Both are keyed on the *blob*, so the same file named twice stays legal (`adding_two_paths_with_identical_bytes_keeps_the_id_and_the_items_own_fingerprint_in_step`). `adding_two_different_files_under_one_name_is_refused` pins the creation half, mutation-checked against a founded group so that deleting the guard makes the call succeed and write the bad item rather than failing on a missing catalogue; `downloading_an_item_whose_files_share_a_name_is_refused` pins the download half, and now seeds its item by writing to the catalogue directly, since `library.add` will no longer produce one. **Not checked against an existing item's filenames on the contribute path**, deliberately: a collision already in the document was written by somebody else's call, and refusing a contribution over it would turn a repair into a dead end. The refusal names `file`, which is the way through: a hash says which of two identically-named files is wanted where their names cannot. Writing both under disambiguated names is the more useful answer and wants `FileRecord`'s `disc` and `seq` to do it with — they are the right disambiguator and nothing fills them yet (P2-23), so a hash-prefix suffix would be inventing a second one. The third is a foot-gun rather than a threat under §2 — a `filename` is a string another member's build wrote into the document, and `dest.join("/etc/passwd")` is `/etc/passwd` — and it closes in one line, by reducing the record's filename to its last component before joining and refusing a record that has no last component. **A failure partway through a multi-file download leaves the files already written where they are** and names only what failed — they are complete and verified, deleting them would be the method destroying something on its way out, and asking again writes the rest while refusing those by name. **No deadline**, following `Blobs::fetch`'s own reasoning (P2-22) — a media file is as slow as it is big, and the failure a timeout would guard against does not need one, since an unreachable provider fails rather than hangs. | **"Register as a holder" turned out to be structural rather than an action, and that is the whole of §6.3's third step here.** The fetch lands the bytes in the same `FsStore` that `Catalogue::protocols()`' `BlobsProtocol` serves from, so the node is a provider from the moment the fetch returns, and stays one across a restart because that store is on disk. §5.6's heartbeat is what would *announce* it and is phase 4; there is nothing to announce into, and building an announcement nobody reads would be a commitment made before the thing it describes exists. **Provider selection is every member but this one**, which is only implementable because a provider that does not hold the blob is a provider skipped rather than a download failed — verified first, by probe, rather than assumed: `a_provider_that_does_not_hold_the_blob_is_skipped_rather_than_fatal` puts both kinds of miss (a member serving blobs and holding nothing, and a member nothing can resolve) ahead of the one holder in the list. It is O(members) in the worst case and wrong at the thousands §2 allows — the same shape as the peer-offer sweep already carried out of this phase, and the answer to it is §5.6's availability index rather than a guess here. **The local store is asked before the network is** (`Blobs::has`), which is not an optimisation: `library.download` on the node that added the item, or in a group of one, has an empty provider list and no chance of a fetch succeeding, and mutation-checking `downloading_something_this_node_already_has_asks_nobody` against an unconditional fetch turns it red with exactly that error. **`distlib-net::Blobs` gains `has` and `export` beside `fetch`**, widening that module from fetching to fetching-and-exporting; the division that keeps §5.1's wrap-don't-expose rule intact is by what the store is being *used as* — `distlib-sync` owns it as the thing iroh-docs keeps entry values in and imports a local file for that reason (P2-23's `add_file`), `distlib-net` owns it as the thing media moves in and out of. So `distlib-api` gains a `distlib-net` dependency and `Api` a `blobs` field, with the same harness tax P2-23's `catalogue` field had: `distlib-api/tests/rpc.rs`'s two builders now hand their `MemStore` to both the catalogue and a `Blobs`, one store between them as in production. `NetError::Fetch` moves from `iroh_blobs::Hash` to `ContentHash` and gains `Blob` and `Export` siblings, so the crate's public error type names domain types only. **Export is a copy, and it is load-bearing**: `ExportMode::TryReference` would move the store's own file out to the target, so an operator deleting the book they just read would quietly stop this node serving it — mutation-checked in two places, and both checks needed a blob over the inline threshold to have any teeth at all, because `FsStore` writes a small blob out of its database with nothing to move and nothing to reference. Both tests say so where the size is chosen. **The phase acceptance is 2b-3b, not this PR.** §9's criterion is a joining procedure (`distlib join`, a fresh node, a search, a download, a restart) and the phase doc asks for it through the actual commands beside `founding.rs`, whose process harness lives inside that file and would have to be extracted to be shared — a second, mechanical change on top of a feature PR. What this PR does carry is the half of the criterion that is a claim about `library.download` itself rather than about the procedure: `a_node_that_downloads_a_file_serves_it_after_a_restart` has bob download alice's book, delete his copy of the file, restart onto a fresh port, and then serve it to carol with alice shut down — so the bytes carol gets have nowhere else in the group to have come from. |
| P2-25 | §9's phase-2 acceptance sentence, and 2a-5 / P2-17 | "Node A adds 3 ebooks; node B (fresh join) syncs the catalogue, searches by author, downloads a file, and after restart still serves it." 2a-5 asked a core node for the directory **once**, at follower startup, on P2-17's reasoning that a restarted core node's directory refills from the announcements its reappearance provokes. | **The criterion passes, and getting it to needed a second ask plus a change to what "restart" means.** *One-shot refresh.* `library.download` offers the whole membership as providers (P2-24) and that only works if the endpoint can turn a member id into an address, which two situations defeat: this node never heard the provider announce itself, or it heard an announcement that went stale when the provider restarted on a new port. So a failed fetch now calls `MembershipNode::refresh_addresses` — 2a-5's `Request::Directory`, asked a second time — and retries, once per call rather than per file: a refresh that did not help the first file will not help the fifth. **The trigger is the fetch failing, not "we hold no address for this member"**: a provider that cannot be placed and one placed wrongly fail identically from the caller's side, and only the fetch knows the difference — a peer that restarted on a new port resolves perfectly well, to somewhere nobody is listening. Gating on the address would skip exactly that case, which a run through the failure path shows plainly: it logs `unplaced=0 of=2` while the fetch has just failed. Sitting on the failure path also means the happy path pays nothing, which the first cut of this did not — it refreshed before the first fetch of every download, reachable providers or not. **How wide the hole really is was not established**, and the by-hand re-runs argue it is narrower than first thought: with a core node up, a follower that arrives late gets the earlier one's address from the startup directory ask, and a peer that moves re-announces to everyone already in the swarm — both reached the holder first try. What is certain is the shape of it: one ask, at startup, nothing that asks again, and a restarted core node answering out of an empty directory. **Its limit is inherent**: it asks a core node, so with no core node reachable there is nobody to ask, and it logs that rather than pretending. *Announcement gossip is still the primary mechanism*, and P2-17's claim holds only for a node that is in the swarm when the announcement goes out; nothing repeats one for an arrival that came later. *A restart has to be orderly.* The membership log survives a kill — redb commits per entry — but the blob store's metadata is written when the router closes it on the way out, so a node killed outright comes back holding a downloaded blob's `.data` and `.obao4` on disk with no record that it holds them: `has()` reports false for bytes it physically has, and the node re-fetches what it already had. That is iroh-blobs' flush cadence rather than anything in this workspace, and it is why the process harness stops a node with a signal and waits for it. **The loss is specific to fetched blobs**: measured, a blob that `library.add` imported is still held after `kill -9`, and one the downloader fetched is not — so the two write paths do not commit their metadata alike, and it is the download half that depends on the close. **`distlib run` now answers `SIGTERM` as well as Ctrl-C**, which is what makes this survivable in practice: a supervisor stops a service with `SIGTERM`, whose default disposition is to kill the process where it stands, so a systemd- or Docker-run node would have paid this cost on every ordinary restart. The handlers are installed before the runtime is built rather than beside the wait, so a stop that arrives *during* startup is remembered and acted on instead of being fatal, and the log names which signal arrived. A hard `kill -9` is still a hard kill and always will be — nothing can answer that one. *A side effect worth knowing about, found by making the harness stop nodes properly.* A core node that closes cleanly and comes back on a **different port** is often reachable again without anybody running `core set`: the catalogue's document sync dials every peer within milliseconds of startup, and an incoming connection is how a peer learns the address to answer on. Measured at roughly one run in five under load, which is the point — it is a race, not a mechanism, and P1-23's `core set` remains the only way that is *reliably* the way back. A node that was killed does not get this: its peers never dropped the path they held for it. `founding.rs`'s renumbered-machine test therefore crashes that node rather than stopping it, which is also the more faithful model of a machine that was renumbered. *The acceptance is split.* `crates/distlib/tests/acceptance.rs` drives §9's sentence through the commands and asserts every clause but the last one taken literally: it checks that the restarted node still **holds** the file (`had it`), not that a third member fetches it from him. That half needs a third member holding a *fresh* address for the restarted one at the moment the only core node is deliberately stopped, which today turns entirely on gossip ordering — a test written for it would pass on timing rather than on the property. It is pinned twice elsewhere instead: in process, watching the fetch, by `download.rs`'s `a_node_that_downloads_a_file_serves_it_after_a_restart`, and by hand in `docs/manual-check.md` §10, which runs the whole sentence across three processes and was executed end to end for this change. |

## Phase 3 — API + UI

*In progress.* The sequencing plan is [`plan-phases/phase-3-api-ui.md`](plan-phases/phase-3-api-ui.md);
as in phase 2, deviations from the design land here, in the PR that causes them.

| # | §  | Doc says | We do | Why |
|---|---|---|---|---|
| P3-1 | §2, P0-6 | platforms are Linux, Windows and macOS | **CI tests all three.** Closes P0-6. | Deferred twice, and widened now because phase 3 adds a JS build, an embedded UI and release artefacts, and a platform problem found under those would be found tangled up with them. **What the matrix found**: macOS passed as it stood. Windows found a product bug that was never Windows-specific (P3-2), Raft timing too tight for anything but a quiet LAN (P3-4), and that `run` could not be stopped in an orderly way there (P3-5). **The process harness stops a Windows node with Ctrl-Break**, where Unix gets `kill -INT`: each node is spawned with `CREATE_NEW_PROCESS_GROUP`, so the event can be addressed to it alone, and sent with `GenerateConsoleCtrlEvent`. That call has no safe wrapper, so **the workspace lint is `unsafe_code = "deny"` rather than `"forbid"`** — `forbid` cannot be lifted for a single item — and that one call is the only `#[allow(unsafe_code)]` in the tree, with a `SAFETY` comment saying why it holds. `windows-sys` is a Windows-only dev-dependency, pinned at the version tokio's `mio` already brings in. Before this, the harness killed Windows nodes outright, and the acceptance test's restarted node re-fetched a file it had downloaded (P2-25); with an orderly stop it is checked on all three platforms. CI also runs nextest with `--no-fail-fast` now: the first Windows run stopped at 13 tests of 328, which is a count of what was hidden rather than a finding. |
| P3-2 | §9.1 | — | **Logs go to stderr; stdout carries only a command's answer.** Keeping a node's log is `distlib run 2> node.log`. | Found by the Windows run, and not a Windows bug: logs shared stdout with output, so any warning logged during a one-shot command came out above its answer. On Windows reading the token always warns (P3-3), so `distlib ticket` printed that warning where the ticket belonged, and a script reading the first line — the test harness, here — handed the warning to `join`. On Linux it only needed a warning to happen. Pinned by `a_commands_answer_is_not_mixed_with_its_log`, which forces a debug line during `members` and fails with the writer set back to stdout. |
| P3-3 | P1-25 | "the token lives in `<data-dir>/api.token` at mode 0600"; the node key likewise | **Unix only. On Windows neither file's privacy is enforced or checked**, and `private_file` logs that it is not. Not fixed here. | The default data directory on Windows is under `%APPDATA%`, which inherits the profile's ACL — the user, SYSTEM and Administrators — so the default is private without anything of ours doing it, roughly as a 0700 home directory would be on Unix. What is unprotected is an operator pointing `--data-dir` at a shared folder, and the warning already says so. Enforcing it would mean writing an ACL through the Windows security API — `unsafe` FFI of roughly a hundred lines, or a new dependency — to cover a case that the default avoids and the log already names. Worth revisiting with the release build in 3c, if Windows is advertised as a server platform rather than a desktop one. |
| P3-4 | §4.5 | openraft named; its timing not discussed | **250ms heartbeat, 1–2s election timeout**, where openraft defaults to 50ms and 150–300ms | The defaults suit voters on one LAN. Voters here are friends' machines, possibly behind a relay, on a home uplink, busy with other things, and a single stall past the election timeout deposes the leader and fails whatever proposals were in flight — `propose` gives up after three attempts at a leader that keeps changing. Found as a Windows CI failure in `a_demoted_node_catches_up_on_what_it_missed`, then reproduced and measured on Linux by running 24 copies of that test beside 24 CPU hogs: **on the defaults 22 of 24 freshly founded groups held another election within three seconds, up to term 5, and 2 failed; on these values all 24 stayed at term 1 and none failed.** Over ten such rounds the failure count was 4 of 240 against 0 of 240. The price is that a leader which really has died is replaced after one to two seconds rather than a fraction of one; nothing waiting on a membership log notices that. |
| P3-5 | P2-25 | — | **On Windows `run` stops on Ctrl-C, Ctrl-Break, the console closing and the machine shutting down**, all listened for from startup as the Unix signals are. | It listened for Ctrl-C alone, and only once startup was over. Ctrl-Break is the one that matters most: anything that stops one child without stopping itself has to start it in a process group of its own, and such a process has Ctrl-C disabled — so without it every such stop was fatal and cost what P2-25 says an abrupt stop costs. Console close and shutdown are the other ways Windows tells a console process to go, and it kills the process a few seconds after either, so both start the shutdown at once. Pinned by `a_node_sent_ctrl_break_shuts_down_cleanly`, the counterpart of the Unix `SIGTERM` test and sharing its body. **Not covered: a Windows service.** The service control manager stops a service by a route none of these are; running `distlib` as one needs a service wrapper, which is the release build's question (3c). |

## Phase 4 — Availability + community metadata

*Not started.*

## Phase 5 — Custodianship & quotas

*Not started.*

## Phase 6 — Hardening & release

*Not started.*
